# 面试计划进度

## 2026-06-06
- 读取 `$karpathy-guidelines` 和 `$planning-with-files` 技能说明。
- 读取岗位截图对应的粘贴文本。
- 读取已有工作区调查结论，确认主项目为 `spring_code/bit-forum-spring`。
- 读取 Spring 项目 README、项目改善计划和 Maven 依赖。
- 将规划文件从“文件夹调查计划”切换为“Java 实习面试通关计划”。
- 记录一个恢复脚本路径错误：默认 `.codex` 下不存在 `planning-with-files` 脚本。
- 阅读用户、文章、评论、Redis、RabbitMQ、JWT、异常处理和 Docker 相关代码。
- 提取关键面试证据行号，并记录 PowerShell `rg` 引号错误的规避方式。
- 完成岗位-项目匹配判断：主攻 Java/Spring Boot/MySQL/Redis/Docker/联调排查，弱项 SpringCloud/Python 采用诚实补位策略。
- 准备最终输出：14 天通关计划、项目讲法、必修补强项、Top 面试题和投递策略。
- 根据用户偏好，将 `spring_code/bit-forum-spring/项目改善与提升计划.md` 改造成“分模块执行手册”，覆盖 M1-M6：安全、数据一致性、部署、测试、接口规范、性能架构亮点。
- 确认后续协作方式：用户自己敲代码，Codex 负责解释、拆步骤、review 和验证；未经明确授权不直接修改业务代码。
- 当前下一步起点：M1 安全基础，建议先做“注册接口不返回密码哈希”。
- M1-1 用户自行新增 `UserResponse` DTO，并将 `UserController.register` 从返回 `Result<User>` 改为 `Result<UserResponse>`；注册响应不再直接返回 `User.password`。
- 运行 `mvn test` 验证通过：1 个测试成功，构建成功。日志中出现 RabbitMQ 未启动的连接拒绝信息，但未导致测试失败，后续可在 M4 测试隔离处理。
- M1-2 用户自行新增 `JwtProperties`，将 `JwtUtil` 从静态硬编码工具类改为 Spring Bean，并在 `UserController`、`LoginInterceptor` 中注入使用。
- 再次运行 `mvn test` 验证通过：1 个测试成功，构建成功；本次 RabbitMQ 可连接，测试日志无 RabbitMQ 连接失败导致的异常。
- 根据用户要求，为 M1-1/M1-2 已改代码补充理解型注释；后续每个小任务完成后都执行“补注释 + 测试验证”步骤。
- M1-3 用户自行修改 `GlobalExceptionHandler`：未知异常不再返回 `e.getMessage()`，改为服务端 `log.error` 记录细节，客户端返回固定提示。
- 用户自行运行 `mvn test` 验证通过：1 个测试成功，构建成功，完成时间 2026-06-07T14:31:33+08:00；随后为异常处理代码补充理解型注释。
- M2-1 用户自行修改 `ArticleController`：`view`、`like`、`unlike` 在操作 Redis 前先通过 `articleService.findById(articleId)` 校验文章存在，避免给不存在文章创建浏览量或点赞数据。
- 为 M2-1 补充理解型注释，并运行 `mvn test` 验证通过：1 个测试成功，构建成功，完成时间 2026-06-07T14:38:02+08:00。
- M2-2 用户自行修改 `CommentService` 和 `CommentController`：发布评论前先校验文章存在，Service 返回 boolean 表示业务是否成功，Controller 根据结果返回“文章不存在”或“评论发布成功”。
- 为 M2-2 补充理解型注释，并运行 `mvn test` 验证通过：1 个测试成功，构建成功，完成时间 2026-06-07T14:47:22+08:00。
- M2-3 用户自行修改删除链路：`CommentService.deleteByArticleId` 按文章清理评论，`RedisService.deleteArticleData` 清理浏览量/点赞/热榜数据，`ArticleService.delete` 使用 `@Transactional` 串起权限校验、删评论、删文章、清 Redis。
- Review 时发现并修正两个关键问题：`QueryWrapper<Component>` 应为 `QueryWrapper<Comment>`；Redis 点赞 Key 应为 `article:{id}:likes`，不能漏掉冒号。
- 为 M2-3 补充理解型注释，并运行 `mvn test` 验证通过：1 个测试成功，构建成功，完成时间 2026-06-07T15:02:43+08:00。M2 数据一致性阶段完成。
- M3-1 用户新增 Flyway 依赖和 `src/main/resources/db/migration/V1__init_schema.sql`，包含 `user_info`、`article`、`comment` 三张表及基础索引。
- 首次 review 发现迁移文件名 `V1__init_schaema.sql` 拼写不规范，用户已改为 `V1__init_schema.sql`；目录中确认只剩一个 V1 迁移文件。
- 运行 `mvn test` 触发 Flyway 依赖下载，但 Maven Central TLS 握手失败：`Remote host terminated the handshake`，重试提权网络后仍失败。当前阻塞点是依赖下载，不是 SQL 编译或迁移执行。
- 已为 `V1__init_schema.sql` 补充理解型注释；待网络可下载 `org.flywaydb:flyway-core` 和 `org.flywaydb:flyway-mysql` 后继续验证。
- 用户本机随后成功下载 Flyway 依赖，但测试报 Flyway 接管旧库错误：`Found non-empty schema(s) bit_forum but no schema history table`。
- 用户采用方式 A，在 `application.yml` 配置 `spring.flyway.baseline-on-migrate: true` 让 Flyway 接管已有数据库；再次运行 `mvn test` 验证通过：1 个测试成功，构建成功，完成时间 2026-06-07T15:16:55+08:00。
- 为 `application.yml` 的 Flyway baseline 配置补充理解型注释。M3-1 数据库初始化脚本任务完成。
- 用户决定跳过 Maven Wrapper，优先处理更适合面试展示的 Docker 多阶段构建。
- M3-2 用户自行将 `Dockerfile` 改为多阶段构建：builder 阶段使用 `maven:3.9.9-eclipse-temurin-17` 从源码打包，runtime 阶段使用 `eclipse-temurin:17-jre` 运行 JAR。
- 用户新增 `.dockerignore`；首次放在 `spring_code/.dockerignore`，review 后移动到正确构建上下文 `spring_code/bit-forum-spring/.dockerignore`，父目录多余文件已不存在。
- 运行 `docker compose config` 校验通过；Docker 输出 `C:\Users\10603\.docker\config.json: Access is denied` 警告，但 Compose 配置已正常解析。
## 2026-06-07
- M4 自动化测试继续推进：用户新增 `ArticleServiceTest.nonAuthorShouldNotDeleteArticle`，覆盖“非作者不能删除文章”的权限校验。
- 为新增测试补充理解型注释：先造作者文章，再用其他用户删除，断言抛出“只能删除自己的文章”，并回查数据库确认文章没有被误删。
- 运行 `mvn test` 验证通过：Tests run: 4, Failures: 0, Errors: 0, Skipped: 0，BUILD SUCCESS。
- M4 自动化测试继续推进：用户新增 `CommentServiceTest.publishShouldFailWhenArticleNotExists`，覆盖“评论不存在文章失败”和“不插入脏评论”。
- 为新增测试补充理解型注释：先确认文章不存在，再调用评论发布，断言返回 false，并回查 comment 表确认没有插入数据。
- 运行 `mvn test` 验证通过：Tests run: 5, Failures: 0, Errors: 0, Skipped: 0，BUILD SUCCESS。
- 用户决定今天学习到此为止；新增 `spring_code/bit-forum-spring/2026-06-07学习总结.md`，总结今日完成内容、核心测试、部署排查、新名词解释和明天起点。
- 更新 `task_plan.md`：M1、M3、M4 标记完成；下一步明确为 M5 接口规范化，从“注册/登录 Request DTO + 参数校验”开始。

## 2026-06-08
- 从 M5 接口规范化开始；本轮 session-catchup 默认 `.codex` 路径仍不存在，改用 `C:\Users\10603\.agents\skills\planning-with-files\scripts\session-catchup.py` 恢复上下文。
- 用户新增 `UserRegisterRequest` 和 `UserLoginRequest`，并将 `UserController.register/login` 从 `@RequestParam` 改为 `@Valid @RequestBody` Request DTO。
- 为两个 Request DTO 和 `UserController` 入参改造补充理解型注释：解释入参 DTO、`@RequestBody`、`@Valid` 和避免参数散落在 URL 中。
- 运行 `mvn test` 验证通过：Tests run: 5, Failures: 0, Errors: 0, Skipped: 0，BUILD SUCCESS。下一步建议补接口层参数校验测试。
- 用户新增 `UserControllerTest.registerShouldFailWhenUsernameIsBlank`，使用 `MockMvc` 模拟注册 JSON 请求，验证 `@Valid` 与 `GlobalExceptionHandler` 能返回“用户名不能为空”。
- 本轮 Controller 测试踩坑并修正：测试文件误命名为 `UserController.java` 导致同名冲突；误导入 Tomcat `MediaType`；`@WebMvcTest` 与启动类 `@MapperScan` 冲突导致 mapper 缺少 `sqlSessionFactory`；空字符串同时触发 `@NotBlank` 和 `@Size`，最终改用 `username: null` 只触发非空校验。
- 为 `UserControllerTest` 补充理解型注释并整理格式；运行 `mvn test` 验证通过：Tests run: 6, Failures: 0, Errors: 0, Skipped: 0，BUILD SUCCESS。
- 用户继续新增 `UserControllerTest.loginShouldFailWhenPasswordIsBlank`，覆盖登录接口 `UserLoginRequest.password` 的非空校验；用户本地运行 `mvn test` 验证通过。
- 用户更新协作规则：后续用户敲完代码后，Codex 负责检查；若无问题，Codex 补理解型注释并直接运行 `mvn test`，不再要求用户自己测试。
- 用户更新环境协作规则：如果 Redis 或 RabbitMQ 没开，Codex 只提醒用户自行启动，不直接启动 Docker/本地服务。
## 2026-06-08 补充
- 用户确认 Redis 和 RabbitMQ 已自行启动；Codex 按新的协作规则直接运行完整 `mvn test`。
- 验证结果：`Tests run: 7, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`，说明当前 M5 的 Request DTO、参数校验、Controller 测试和 LoginResponse DTO 改造均能通过现有测试。
- 需要记住：后续如果 Redis 或 RabbitMQ 未启动，Codex 只提醒用户启动，不主动启动服务。
- M5 继续补充 `UserControllerTest.loginShouldReturnTokenAndUserInfoWhenSuccess`，用 Mockito 模拟登录成功和 JWT 生成，验证登录接口返回 `LoginResponse` 中的 `token/userId/username`。
- Codex 为新增成功登录测试补充理解型注释，并运行完整 `mvn test`；验证结果：`Tests run: 8, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。
- M5 继续推进文章发布接口规范化：用户新增 `ArticlePublishRequest`，并将 `ArticleController.publish` 从直接接收数据库实体 `Article` 改为接收发布请求 DTO。
- Codex 为 `ArticlePublishRequest` 和发布接口补充理解型注释，并运行完整 `mvn test`；验证结果：`Tests run: 8, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。
- M5 继续推进文章更新接口规范化：用户新增 `ArticleUpdateRequest`，并将 `ArticleController.update` 从多个 `@RequestParam` 改为 `@Valid @RequestBody ArticleUpdateRequest`。
- Codex 删除本次改动产生的无用 `ResponseBody` 导入，为更新 DTO 和 Controller 更新逻辑补充理解型注释，并运行完整 `mvn test`；验证结果：`Tests run: 8, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。
- M5 收尾推进评论发布接口规范化：用户新增 `CommentPublishRequest`，并将 `CommentController.publish` 从 `@RequestParam articleId/content` 改为 `@Valid @RequestBody CommentPublishRequest`；查询评论列表接口继续保留 `@RequestParam articleId`。
- Codex 为评论发布 DTO 和 Controller 调用处补充理解型注释，并运行完整 `mvn test`；验证结果：`Tests run: 8, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。M5 的主要 DTO/参数校验改造已完成。
## 2026-06-09
- 开始今日学习。根据昨日进度，M5 的主要 DTO/参数校验改造已完成，今天进入 M6 性能与架构亮点。
- 当前 M6 第一阶段建议从 Redis ZSet 热榜开始：项目中 `RedisService.incrHot/getHotList` 已存在，但 `/api/article/hot` 当前仍遍历 MySQL 所有文章计算热度；下一步改为浏览/点赞时更新 ZSet 分数，热门接口直接读取 ZSet。
- 用户完成 Redis ZSet 热榜第一版改造：浏览文章时热度 +1，点赞时热度 +3，`/api/article/hot` 改为读取 Redis ZSet 前 10 名并组装 `HotArticle` 返回。本步按用户新规则暂不运行 `mvn test`，仅补充理解型注释。
- 用户将 `RedisService.incrHot(articleId)` 优化为 `increaseHot(articleId, score)`，浏览调用 `increaseHot(articleId, 1)`，点赞调用 `increaseHot(articleId, 3)`；Codex 检查确认旧单参数调用已清理，并补充理解型注释。本小步暂不运行 `mvn test`。
- M6 补充 Redis ZSet 热榜测试：在 `RedisServiceTest.hotListShouldReturnHigherScoreArticleFirst` 中验证低热度文章 +1、高热度文章 +3 后，`getHotList(2)` 返回的第一名是高热度文章。
- Codex 将测试变量从容易误导的 `hotLList/hotList` 整理为 `hotSet`，并补充详细注释解释：Redis ZSet 查询结果用 `Set<ZSetOperations.TypedTuple<String>>` 承接，每个 tuple 包含 value(articleId) 和 score(热度分)。完整 `mvn test` 通过：`Tests run: 9, Failures: 0, Errors: 0, Skipped: 0`。
- M6 开始文章列表分页：用户新增 `MybatisPlusConfig` 配置 `MybatisPlusInterceptor + PaginationInnerInterceptor(DbType.MYSQL)`。首次编译报 `PaginationInnerInterceptor` 找不到，定位为 MyBatis-Plus 3.5.9 需要补 `mybatis-plus-jsqlparser` 依赖。
- 用户在 `pom.xml` 补充 `com.baomidou:mybatis-plus-jsqlparser:3.5.9` 后，Codex 运行 `mvn -q -DskipTests compile` 通过，并为分页配置类补充理解型注释。
- 用户在 `ArticleService` 新增分页查询方法；Codex 将方法名调整为 `pageArticles(pageNum, pageSize)`，并补充注释解释 `Page` 参数和 `selectPage` 会返回当前页数据及分页元信息。本小步暂不运行测试，等待 Controller 分页接口接入后统一验证。
- 用户在 `ArticleController` 新增 `/api/article/page` 分页接口，支持 `pageNum/pageSize` 默认参数；Codex 将方法名调整为 `pageArticles`，补充注释说明默认分页和 `Page` 返回元信息。
- 完整运行 `mvn test` 验证通过：`Tests run: 9, Failures: 0, Errors: 0, Skipped: 0`。日志显示 Spring MVC mappings 从 15 增至 16，说明分页接口已被扫描注册。
- M6 RabbitMQ 可靠性小步：用户将 `article.publish.queue` 队列名抽到 `RabbitMQConfig.ARTICLE_PUBLISH_QUEUE`，配置类、生产者 `ArticleService`、消费者 `NotificationListener` 已统一引用常量。
- Codex 同步修正 `ArticleServiceTest` 中 MQ 发送断言，改为引用 `RabbitMQConfig.ARTICLE_PUBLISH_QUEUE`，并为队列名常量补充理解型注释。运行 `mvn test` 时 Java 编译通过，但测试阶段因本地 Redis `6379` 未连接导致 `RedisServiceTest` 失败，日志中 RabbitMQ `5672` 也显示连接拒绝；需用户自行启动 Redis/RabbitMQ 后重跑。
- 用户启动 Redis 和 RabbitMQ 后，Codex 重新运行完整 `mvn test`；验证结果：`Tests run: 9, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。队列名常量化完成。
- M6 RabbitMQ 生产者可靠性继续推进：用户在 `application.yml` 开启 `publisher-confirm-type: correlated`、`publisher-returns: true` 和 `template.mandatory: true`，并在 `RabbitMQConfig` 中给 `RabbitTemplate` 配置 ConfirmCallback 与 ReturnsCallback。
- Codex 修正 `RabbitMQConfig` 中 logger 作用域和 `initRabbitTemplate` 方法名，补充注释解释 ConfirmCallback 负责“消息是否到达 Broker”，ReturnsCallback 负责“消息到达 Broker 后是否成功路由到队列”。完整运行 `mvn test` 通过：`Tests run: 9, Failures: 0, Errors: 0, Skipped: 0`。
- M6 RabbitMQ 消息结构化完成：用户新增 `ArticlePublishMessage`，将文章发布 MQ 消息从 `"文章ID：xx"` 字符串改为包含 `articleId/userId/title/publishTime` 的结构化对象；`NotificationListener` 改为接收 `ArticlePublishMessage`。
- 本步踩坑：`MessageConverter` 一开始误导入 Logback 包，已改为 `org.springframework.amqp.support.converter.MessageConverter`；直接在 `RabbitMQConfig` 注入 `RabbitTemplate` 会和 `MessageConverter` Bean 形成循环依赖，已改为 `RabbitTemplateCustomizer` 配置 confirm/returns 回调。
- Codex 修改 `ArticleServiceTest.publishShouldSaveArticleAndSendMessage`：用 `ArgumentCaptor<ArticlePublishMessage>` 捕获 MQ 消息对象，并断言队列名、`articleId/userId/title/publishTime`。完整运行 `mvn test` 通过：`Tests run: 9, Failures: 0, Errors: 0, Skipped: 0`。
- M6 RabbitMQ 幂等铺垫完成：用户给 `ArticlePublishMessage` 增加 `messageId`，`ArticleService.publish` 使用 `UUID.randomUUID().toString()` 为每条 MQ 消息生成唯一编号，`NotificationListener` 日志增加 `messageId`。Codex 修正日志占位符并补充注释。完整运行 `mvn test` 通过：`Tests run: 9, Failures: 0, Errors: 0, Skipped: 0`。
- M6 RabbitMQ 消费者幂等完成：用户在 `RedisService` 增加 `markMessageProcessed(messageId)`，使用 Redis Set `mq:processed:article_publish` 判断消息是否第一次处理；`NotificationListener` 消费前先调用该方法，重复消息直接记录日志并跳过。Codex 修正变量名、补充注释，并新增 `RedisServiceTest.sameMessageShouldOnlyBeProcessedOnce` 验证第一次返回 true、第二次返回 false。完整运行 `mvn test` 通过：`Tests run: 10, Failures: 0, Errors: 0, Skipped: 0`。
- M6 RabbitMQ 显式路由结构完成：用户在 `RabbitMQConfig` 增加 `ARTICLE_EXCHANGE`、`ARTICLE_PUBLISH_ROUTING_KEY`、`DirectExchange` 和 `Binding`，将发布链路从默认交换机改为 `Producer -> article.exchange -> article.publish routingKey -> article.publish.queue -> Consumer`。`ArticleService.publish` 改为三参数 `convertAndSend(exchange, routingKey, message)`，`ArticleServiceTest` 同步断言 exchange/routingKey 和消息对象。完整运行 `mvn test` 通过：`Tests run: 10, Failures: 0, Errors: 0, Skipped: 0`。
- M6 RabbitMQ 消费者手动 ACK 完成：用户在 `application.yml` 配置 `spring.rabbitmq.listener.simple.acknowledge-mode: manual`，`NotificationListener.handlePublish` 增加 `Channel` 与原始 `Message` 参数，通过 `deliveryTag` 在重复消息和成功处理后 `basicAck`，异常时 `basicNack(requeue=false)`。Codex 修正失败路径避免 nack 后继续 ack，并新增 `NotificationListenerTest.duplicateMessageShouldAckAndSkip` 验证重复消息会 ack 且不会 nack。完整运行 `mvn test` 通过：`Tests run: 11, Failures: 0, Errors: 0, Skipped: 0`。
- M6 RabbitMQ DLQ 配置代码完成：用户在 `RabbitMQConfig` 增加文章发布死信交换机、死信队列和 DLQ binding，并给普通队列添加 `x-dead-letter-exchange` 与 `x-dead-letter-routing-key` 参数。Codex 修正/补充 DLX、DLQ 注释。完整运行 `mvn test` 通过：`Tests run: 11, Failures: 0, Errors: 0, Skipped: 0`；但日志提示本地 RabbitMQ 已存在旧 `article.publish.queue` 且没有 DLX 参数，Broker 拒绝用新参数重声明同名队列，需要用户在 RabbitMQ 管理台删除旧队列或重建本地 RabbitMQ 数据后，DLQ 拓扑才会真正生效。
- 用户在 RabbitMQ 管理台删除旧队列后重新触发项目声明，确认 `article.publish.queue` / `article.publish.dlq` 等新拓扑已生效。DLQ 改造闭环完成。
- M6 RabbitMQ DLQ 监听入口完成：用户在 `NotificationListener` 新增 `handlePublishDeadLetter`，监听 `RabbitMQConfig.ARTICLE_PUBLISH_DLQ` 并记录失败消息的 `messageId/articleId/userId/title`。Codex 补充注释说明该方法是失败消息排查、告警或人工补偿入口。完整运行 `mvn test` 通过：`Tests run: 11, Failures: 0, Errors: 0, Skipped: 0`。

## 2026-06-10
- 确认 M6 可以收口，进入 M7 项目收口与面试表达阶段。
- M7-1 完成 `spring_code/bit-forum-spring/README.md` 重写：将 README 从旧版启动说明整理为当前项目真实状态，覆盖 Docker Compose 启动、本地 Maven 启动、JSON 接口验证流程、API 总览、RabbitMQ 可靠性链路、5 分钟面试演示流程、常用排查命令和项目亮点总结。
- 本次只修改文档和计划记录，没有改 Java 业务代码，因此未运行 `mvn test`；验收方式为检查 README 中的接口路径、请求体、启动命令和 RabbitMQ 链路是否与当前代码一致。
- M7-2 新增 `spring_code/bit-forum-spring/5分钟项目演示稿.md`，将项目演示整理成“开场介绍 -> Docker 启动 -> 注册登录 -> 发布文章 -> Redis 浏览/点赞/热榜 -> 评论一致性 -> RabbitMQ 可靠性 -> 自动化测试 -> 收尾总结”的 5 分钟讲稿，并在 README 的面试演示流程处增加链接。
- M7-3 重写 `spring_code/bit-forum-spring/简历描述.md`：删除旧的代码规模/接口数量等可能失准信息，改为更适合投递的项目概述、简历版描述、7 条可选亮点和面试可展开方向；重点突出 Redis ZSet 热榜、RabbitMQ 可靠性、Flyway/Docker 部署和核心测试。
- M7-4 更新 `spring_code/bit-forum-spring/面试八股问答清单.md`：在文档开头新增“必背 12 问速记版”，优先覆盖项目介绍、登录认证、JWT、BCrypt、DTO 安全、文章存在校验、Redis Set/ZSet、RabbitMQ 可靠性、Docker 和测试；同步修正旧的继续优化回答，改为测试隔离、监控告警、权限/状态码和后续微服务演进。
- M7-5 新增 `spring_code/bit-forum-spring/面试前最终验收清单.md`：覆盖环境检查、自动化测试、Docker 启动、核心接口演示、RabbitMQ 展示、Redis 展示、常见失败排查、面试前复习入口和最终通过标准；README 的面试演示流程处已增加该清单链接。
- M7-6 完成最终真实验收：在 `spring_code/bit-forum-spring` 下运行 `mvn test` 通过，结果为 `Tests run: 11, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`；运行 `docker compose config` 通过，确认 Compose 能解析 app、mysql、redis、rabbitmq 服务、端口映射、环境变量和 `mysql-data` 数据卷。`task_plan.md` 中 M7 已标记完成。
## 2026-06-10 当前项目阅读
- 本轮按用户要求重新通读 `spring_code/bit-forum-spring` 当前状态：读取项目结构、README、pom、Dockerfile、docker-compose、application.yml、Flyway SQL、Controller、Service、Redis、RabbitMQ、JWT、拦截器、异常处理、DTO 和主要测试。
- `docker compose config` 验证通过，Compose 可解析 app/mysql/redis/rabbitmq 服务；当前 MySQL 主机端口实际是 `3306:3306`。
- 运行 `mvn test` 通过：`Tests run: 11, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。
- 发现 README 中仍描述 MySQL 主机端口 `3307`，但当前 `docker-compose.yml` 实际发布 `3306`，需要后续统一文档或配置。
