# 5 分钟项目演示稿

## 使用方式

这份稿子不是背诵全文，而是帮助你形成一条稳定的演示主线。面试时可以按“项目一句话介绍 -> 启动方式 -> 核心功能演示 -> 技术亮点 -> 测试验证 -> 总结”的顺序讲。

如果时间只有 3 分钟，就讲每一步的第一句话；如果面试官感兴趣，再展开“追问补充”。

## 0. 开场介绍

**建议用时：30 秒**

我做的是一个基于 Spring Boot 3 的轻量级论坛系统，主要功能包括用户注册登录、文章发布、评论、浏览量、点赞、热门排行、RabbitMQ 异步通知和 Docker Compose 部署。

这个项目一开始是 JDBC 版本，后面我把它升级成 Spring Boot 版本，并且重点补了安全、数据一致性、接口规范、自动化测试和消息可靠性。比如注册接口不返回密码哈希、JWT 密钥外置、浏览点赞评论前校验文章存在、Redis 做点赞去重和热门排行、RabbitMQ 做异步解耦。

**面试重点：**

- 你不是只做了 CRUD，而是做了项目质量补强。
- 你能讲清楚每个技术为什么用。
- 你能讲清楚自己发现过什么问题、怎么修、怎么验证。

## 1. 启动项目

**建议用时：30 秒**

我这里用 Docker Compose 启动完整环境，里面包括 MySQL、Redis、RabbitMQ 和 Spring Boot 后端服务。

```powershell
docker compose up --build
```

Compose 启动后，后端服务访问 `http://localhost:8080`，RabbitMQ 管理台访问 `http://localhost:15672`。MySQL 在宿主机映射到 `3307`，避免和本机 MySQL 的 `3306` 冲突。

**可以顺手展示：**

```powershell
docker compose ps
```

**追问补充：**

Dockerfile 使用多阶段构建，第一阶段用 Maven 镜像从源码打包，第二阶段用 JRE 镜像运行 jar。这样镜像构建不依赖本机提前生成 `target`，更接近真正的一键部署。

## 2. 注册和登录

**建议用时：45 秒**

先演示用户注册：

```http
POST /api/user/register
```

```json
{
  "username": "testuser",
  "password": "123456"
}
```

注册接口返回的是 `UserResponse`，只包含用户 id、用户名、头像等基础信息，不返回数据库里的 password 哈希。

然后演示登录：

```http
POST /api/user/login
```

```json
{
  "username": "testuser",
  "password": "123456"
}
```

登录成功后返回 token，后续发布文章、点赞、评论等写操作都通过 Header 携带：

```text
Authorization: Bearer <token>
```

**追问补充：**

密码使用 BCrypt 哈希存储，不保存明文。JWT 用来做无状态认证，后端通过拦截器解析 token，把 userId 放到 request attribute 里，Controller 不需要让前端手动传 userId。

## 3. 发布文章

**建议用时：45 秒**

演示发布文章：

```http
POST /api/article/publish
Authorization: Bearer <token>
```

```json
{
  "title": "第一篇文章",
  "content": "这是文章内容"
}
```

这个接口只接收 `ArticlePublishRequest`，前端不能传 `id`、`userId`、浏览量、点赞数这些数据库字段。真正的 userId 来自 JWT，保证文章归属可信。

发布文章成功后，主流程负责保存文章，同时通过 RabbitMQ 发送一条 `ArticlePublishMessage`，通知逻辑异步处理。

**追问补充：**

这里用 RabbitMQ，而不是直接在线程池里异步处理，是因为 MQ 可以持久化消息，也更适合后续拆分服务。线程池只在当前进程内，服务重启后未处理任务可能丢失。

## 4. 浏览、点赞和热门排行

**建议用时：60 秒**

演示浏览文章：

```http
GET /api/article/view?articleId=1
```

浏览前会先查 MySQL，确认文章存在。确认存在后，Redis 浏览量加 1，热门排行分数加 1。

演示点赞文章：

```http
POST /api/article/like?articleId=1
Authorization: Bearer <token>
```

点赞也会先校验文章存在。Redis Set 用来记录这篇文章被哪些用户点过赞，天然可以防止同一个用户重复点赞。点赞成功后，热门排行分数加 3。

演示热门排行：

```http
GET /api/article/hot
```

热门排行从 Redis ZSet 读取前 10 名，不再每次遍历 MySQL 所有文章计算。

**追问补充：**

Redis 里三种结构分工不同：

| 数据结构 | 用途 | 原因 |
| -------- | ---- | ---- |
| String | 浏览量计数 | 适合自增 |
| Set | 点赞用户集合 | 适合去重 |
| ZSet | 热门排行 | 适合按分数排序 |

## 5. 评论和数据一致性

**建议用时：40 秒**

演示发布评论：

```http
POST /api/comment/publish
Authorization: Bearer <token>
```

```json
{
  "articleId": 1,
  "content": "这是一条评论"
}
```

评论发布前会先校验文章是否存在。如果文章不存在，直接返回失败，不会插入脏评论。

删除文章时，项目也会清理关联评论和 Redis 数据，并且删除流程加了事务，避免只删了一部分导致数据不一致。

**追问补充：**

我之前发现浏览、点赞、评论都可能对不存在文章操作，这会导致 Redis 和 MySQL 出现脏数据。所以我把文章存在校验补到了这些入口上，删除文章时也清理评论和 Redis。

## 6. RabbitMQ 可靠性

**建议用时：60 秒**

文章发布后的消息链路是：

```text
ArticleService.publish
  -> article.exchange
  -> article.publish routing key
  -> article.publish.queue
  -> NotificationListener
```

消息体不是简单字符串，而是 `ArticlePublishMessage`，包含：

```text
messageId
articleId
userId
title
publishTime
```

生产者侧开启 Confirm 和 Returns：

- Confirm 判断消息是否到达 Broker。
- Returns 判断消息到达 Broker 后是否成功路由到队列。

消费者侧使用手动 ACK：

- 处理成功：`basicAck`。
- 处理失败：`basicNack(requeue=false)`。
- 失败消息进入 DLQ，方便后续排查或补偿。

为了防止重复消费，我用 Redis Set 记录处理过的 `messageId`。重复消息会直接 ACK 跳过。

**追问补充：**

这里我能讲清楚消息丢失和重复消费两个问题。生产者用 Confirm/Returns 观察发送和路由结果；消费者用手动 ACK 控制确认时机；重复消费用业务唯一 id，也就是 `messageId`，配合 Redis Set 做幂等。

## 7. 自动化测试

**建议用时：30 秒**

最后我会运行：

```powershell
mvn test
```

当前测试覆盖了注册登录参数校验、发布文章发送 MQ、Redis 点赞去重、热门排行、非作者删除失败、评论不存在文章失败、RabbitMQ 重复消息 ACK 等核心规则。

**追问补充：**

我补测试不是为了追求数量，而是覆盖最容易出错、最容易被面试官追问的业务规则。比如“同一个用户重复点赞不能重复增加点赞数”、“非作者不能删除文章”、“评论不能发到不存在的文章下”。

## 8. 收尾总结

**建议用时：30 秒**

这个项目对我最大的价值，是我不只是把接口写通了，而是完整经历了从功能实现到质量补强的过程。

我补过安全问题，比如注册不返回密码哈希、JWT 密钥外置；补过数据一致性问题，比如浏览点赞评论前校验文章存在、删除文章清理评论和 Redis；也补了部署和测试，比如 Flyway 初始化数据库、Docker 多阶段构建、Docker Compose 启动、核心业务自动化测试；最后还把 RabbitMQ 从简单异步通知增强到了包含 Confirm、Returns、手动 ACK、幂等和 DLQ 的可靠性链路。

## 一句话版本

如果面试官只给你 30 秒，可以这样说：

> 我做了一个 Spring Boot 论坛项目，从 JDBC 版升级到 Spring Boot 版，实现了用户登录、文章发布、评论、Redis 点赞和浏览量、Redis ZSet 热门排行、RabbitMQ 异步通知以及 Docker Compose 部署。后续我重点补强了安全、数据一致性、接口规范、自动化测试和消息可靠性，比如注册接口不返回密码哈希、JWT 密钥外置、浏览点赞评论前校验文章存在、RabbitMQ 使用 Confirm/Returns、手动 ACK、幂等消费和 DLQ 处理失败消息。
