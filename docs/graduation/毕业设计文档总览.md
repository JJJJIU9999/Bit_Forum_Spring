# 毕业设计文档总览

## 项目名称

基于 Spring Boot 与 React 的社区交流平台设计与实现

## 文档定位

本文是 `docs/graduation/README.md` 的中文替代文件，用于记录毕业设计改造的总体目标、当前能力、模块路线和文档入口。

项目当前不是要重构成微服务，也不是单纯堆技术，而是在现有 `bit-forum-spring` 轻量论坛基础上，逐步补齐社区系统的完整业务闭环：

```text
内容发布
-> 板块组织
-> 审核发布
-> 用户互动
-> 通知反馈
-> 举报治理
-> 后台统计
-> 用户个人中心
```

## 项目路径

```text
D:\ClaudeCode\BitFrom\spring_code\bit-forum-spring
```

## 当前分支

```text
feat/frontend-refactor（基于 graduation-design）
```

开发约束：

- 不在 `main` 分支直接开发毕业设计功能。
- 不新建仓库，不修改 Git 远程地址。
- 不删除或回滚已提交的 M1-M12 功能和当前用户学习文档。
- 不修改 Flyway 已存在的历史迁移脚本，只新增迁移。
- 不提交真实密码、个人信息、本地环境配置、简历 PDF、个人学习记录。

## 技术栈

| 类别 | 技术 |
| --- | --- |
| 后端语言 | Java 17 |
| 后端框架 | Spring Boot 3.4.5 |
| ORM | MyBatis-Plus 3.5.9 |
| 数据库 | MySQL 8 |
| 数据库迁移 | Flyway |
| 缓存 | Redis 7 |
| 消息队列 | RabbitMQ 3 |
| 认证 | JWT + BCrypt |
| 参数校验 | Jakarta Validation |
| 测试 | JUnit 5 + Spring Boot Test + Mockito |
| 部署 | Docker Compose |
| 前端 | React + Vite + Axios + 普通 CSS |

## 当前已实现能力

### 用户与认证

- 用户注册。
- 用户登录。
- BCrypt 密码哈希。
- JWT 登录认证。
- `LoginInterceptor` 校验写接口。
- 注册响应脱敏，不返回 `password`。
- 用户角色：`USER` / `ADMIN`。
- 用户状态：启用 / 禁用。
- 禁用用户不能重新登录。

### 管理员权限

- `AdminInterceptor` 拦截 `/api/admin/**`。
- 从 JWT 获取 `userId`。
- 回查数据库校验用户是否存在。
- 校验用户状态是否启用。
- 校验用户角色是否为 `ADMIN`。

### 文章、评论与 Redis

- 文章发布、修改、删除、详情、分页。
- 评论发布与查询。
- 浏览量统计。
- 点赞与取消点赞。
- 热门文章排行。
- 浏览、点赞、评论前校验文章存在。
- 删除文章时清理评论与 Redis 数据。

Redis 当前用途：

- String：文章浏览量。
- Set：点赞去重。
- ZSet：热门文章排行。
- Set：RabbitMQ 消费幂等 `messageId`。

### RabbitMQ

- 文章发布异步消息。
- 生产者 ConfirmCallback。
- ReturnsCallback。
- `mandatory` 投递失败回调。
- 消费者手动 ACK。
- `basicNack(requeue=false)`。
- DLX / DLQ。
- Redis 幂等消费。

### 管理员模块

- 管理员健康检查。
- 管理员分页查看文章。
- 管理员删除任意文章。
- 管理员分页查看评论。
- 管理员删除任意评论。
- 管理员分页查看用户。
- 管理员启用 / 禁用用户。
- 管理员不能禁用自己。
- 用户列表脱敏，不返回 `password`。

### M1 板块分类

M1 当前已完成实现和验证，已提交到毕业设计分支历史。

已完成：

- 新增 `category` 表。
- 新增 `article.category_id`。
- 已有文章迁移到默认板块。
- `article.category_id` 最终为 `NOT NULL`。
- 新增 Category Entity / Mapper / DTO / Service。
- 新增公共板块接口。
- 新增管理员板块管理接口。
- 文章发布必须选择启用板块。
- 文章分页支持按板块筛选。
- 文章响应补充 `categoryName`。
- 前端文章列表支持板块筛选。
- 前端发布文章支持选择板块。
- 管理员面板新增板块管理。

验证记录：

- `npm run build` 通过。
- `mvn test` 通过，46 个测试全部通过。

详细文档：

```text
docs/graduation/m1-category/task_plan.md
docs/graduation/m1-category/findings.md
docs/graduation/m1-category/progress.md
```

### M6 管理员数据统计看板

M6 当前已完成实现和验证，已提交到毕业设计分支历史。

已完成：

- 新增管理员数据看板接口 `GET /api/admin/dashboard/summary`。
- 新增 dashboard DTO、`AdminDashboardService` 和 `AdminDashboardController`。
- 统计用户、文章状态、评论、板块、收藏、通知和举报治理数据。
- 支持文章、评论、举报的今日新增和最近 7 天新增统计。
- 支持 Redis 热门文章 Top 10，且只返回仍为 `PUBLISHED` 的文章。
- Redis 无热门数据或异常时，热门文章返回空数组，不影响整体统计。
- 前端管理员面板新增“数据看板”页签。

验证记录：

- `mvn test` 通过，112 个测试全部通过。
- `npm run build` 通过，Vite 构建 97 个模块。

详细文档：

```text
docs/graduation/m6-dashboard/task_plan.md
docs/graduation/m6-dashboard/findings.md
docs/graduation/m6-dashboard/progress.md
```

### M7 用户主页与个人资料

M7 当前已完成实现和验证，已提交到毕业设计分支历史。

已完成：

- 新增 `nickname`、`bio` 用户资料字段，头像继续使用 `avatar` URL 字符串。
- 新增 Flyway 迁移 `V9__add_user_profile_fields.sql`，未修改历史迁移 V1-V8。
- 新增当前登录用户资料查询接口 `GET /api/user/profile`。
- 新增当前登录用户资料更新接口 `PUT /api/user/profile`，只允许更新 `avatar`、`nickname`、`bio`。
- 新增公开用户主页接口 `GET /api/users/{userId}/profile`，不返回 `password`。
- 新增公开用户文章分页接口 `GET /api/users/{userId}/articles`，只返回 `PUBLISHED` 文章。
- 前端“我的”页面新增“个人资料”页签。
- 文章详情页新增“查看作者主页”入口。
- 前端新增公开用户主页页面，继续使用现有 `activePage` 字符串切换。

验证记录：

- `mvn "-Dtest=UserServiceTest,UserProfileControllerTest,ArticleServiceTest" test` 通过，40 个测试全部通过。
- `npm run build` 通过，Vite 构建 117 个模块。
- `mvn -DskipTests compile` 通过。
- `mvn test` 通过，130 个测试全部通过。

详细文档：

```text
docs/graduation/m7-user-profile/task_plan.md
docs/graduation/m7-user-profile/findings.md
docs/graduation/m7-user-profile/progress.md
```

### M8 Swagger / OpenAPI 接口文档

M8 当前已完成实现和验证，已提交到毕业设计分支历史。

已完成：

- 新增 `springdoc-openapi-starter-webmvc-ui:2.8.9`。
- 新增 `OpenApiConfig`，配置接口文档标题、描述、版本和 JWT Bearer Token 认证说明。
- 提供 OpenAPI JSON：`/v3/api-docs`。
- 提供 Swagger UI：`/swagger-ui/index.html`。
- 给主要 controller 补充 `@Tag` 和关键 `@Operation`。
- 给关键请求 DTO 补充少量 `@Schema`。
- 新增 `OpenApiControllerTest` 验证 OpenAPI JSON 和 Swagger UI 可访问。
- 本轮没有新增数据库迁移，当前最新迁移仍为 `V9__add_user_profile_fields.sql`。

验证记录：

- `mvn "-Dtest=OpenApiControllerTest" test` 通过，2 个测试全部通过。
- `mvn -DskipTests compile` 通过。
- `mvn test` 通过，132 个测试全部通过。
- `npm run build` 通过，Vite 构建 117 个模块。

详细文档：

```text
docs/graduation/m8-openapi/task_plan.md
docs/graduation/m8-openapi/findings.md
docs/graduation/m8-openapi/progress.md
```

### M9 关注与粉丝

M9 当前已完成实现和验证，已提交到毕业设计分支历史。

已完成：

- 新增 `user_follow` 关注关系表。
- 新增 Flyway 迁移 `V10__add_user_follow.sql`，未修改历史迁移 V1-V9。
- 新增 `UserFollow` Entity、`UserFollowMapper`、`UserFollowService`、`UserFollowController`。
- 新增 `FollowStatsResponse`、`FollowUserResponse` DTO。
- 新增关注、取消关注、粉丝列表、关注列表、关注统计接口。
- 将 `POST/DELETE /api/users/{userId}/follow` 接入现有 `LoginInterceptor`。
- 扩展 M7 公开用户主页响应，新增 `followingCount`、`followerCount`、`followedByCurrentUser`。
- 公开主页支持匿名访问；带有效 Bearer Token 时返回当前登录用户是否已关注该主页用户。
- 前端公开用户主页新增关注/取消关注按钮、关注数、粉丝数、粉丝列表和关注列表。
- 补充 M9 服务层、控制器、公开主页和 OpenAPI 回归测试。

验证记录：

- 聚焦测试 `mvn "-Dtest=UserFollowServiceTest,UserFollowIntegrationTest,UserFollowControllerTest,UserServiceTest,UserProfileControllerTest,OpenApiControllerTest" test` 通过，36 个测试全部通过。
- 后端编译 `mvn -DskipTests compile` 通过。
- 完整后端测试 `mvn test` 通过，151 个测试全部通过。
- 前端 `npm run build` 通过，Vite 构建 117 个模块。

详细文档：

```text
docs/graduation/m9-follow/task_plan.md
docs/graduation/m9-follow/findings.md
docs/graduation/m9-follow/progress.md
```

### M10 文件上传

M10 当前已完成实现和验证，已提交到毕业设计分支历史。

已完成：

- 新增 Flyway 迁移 `V11__add_file_upload_fields.sql`，为 `article` 增加 `cover_url` 字段，未修改历史迁移 V1-V10。
- 新增 `FileUploadResponse`、`FileUploadService`、`FileUploadController`。
- 新增登录上传接口 `POST /api/upload/avatar`，用于上传头像图片。
- 新增登录上传接口 `POST /api/upload/article-cover`，用于上传文章封面图片。
- 上传文件保存到本地 `uploads/avatar/` 和 `uploads/article-cover/`。
- 新增 `/uploads/**` 静态资源映射，返回 URL 形如 `/uploads/avatar/{uuid}.png`、`/uploads/article-cover/{uuid}.jpg`。
- 上传只允许 `image/jpeg`、`image/png`、`image/webp`，头像限制 2MB，文章封面限制 5MB。
- 使用 UUID 生成最终文件名，避免使用原始文件名作为保存名，并加入路径 normalize 校验。
- `.gitignore` 新增 `uploads/`，避免提交本地用户上传文件。
- `Article`、文章发布/草稿/更新 DTO 和 `ArticleService` 已支持 `coverUrl`。
- 头像上传成功后写入 M7 个人资料表单的 `avatar` 字段，继续复用 `PUT /api/user/profile` 保存资料。
- 文章封面上传成功后写入 `coverUrl`，并随 M2 提交审核、保存草稿、更新草稿和文章更新流程传给后端。
- 前端发布页、我的文章页、文章卡片和文章详情页已支持封面上传、预览和展示。
- 补充 M10 上传服务、上传控制器、文章服务和 OpenAPI 回归测试。

验证记录：

- 聚焦测试 `mvn "-Dtest=FileUploadServiceTest,FileUploadControllerTest,ArticleServiceTest,ArticleControllerTest,OpenApiControllerTest" test` 通过，48 个测试全部通过。
- 完整后端测试 `mvn test` 通过，164 个测试全部通过。
- 后端编译 `mvn -DskipTests compile` 通过。
- 前端 `npm run build` 通过，Vite 构建 118 个模块。
- `git diff --check` 通过，仅有 LF/CRLF warning。

详细文档：

```text
docs/graduation/m10-file-upload/task_plan.md
docs/graduation/m10-file-upload/findings.md
docs/graduation/m10-file-upload/progress.md
```

### M12 Actuator 健康检查

M12 当前已完成实现和验证，已提交到毕业设计分支历史。

已完成：

- 新增 `spring-boot-starter-actuator` 依赖。
- 新增 management 配置，仅暴露 `/actuator/health` 和 `/actuator/info`。
- 匿名 `/actuator/health` 不显示组件明细，避免暴露敏感环境细节。
- 将原 `/api/admin/health` 权限探活桩接口升级为管理员健康检查聚合接口。
- 新增 `AdminHealthResponse` 和 `HealthComponentStatus` DTO。
- 新增 `AdminHealthService`，聚合应用、MySQL、Redis、RabbitMQ 健康状态。
- MySQL 使用 `SELECT 1` 轻量检查。
- Redis 使用 `PING` 轻量检查。
- RabbitMQ 使用连接工厂创建轻量连接检查。
- 单个组件异常时返回 `DOWN`，接口仍返回统一 `Result.ok`，不抛出不可控 500。
- `/api/admin/health` 继续位于 `/api/admin/**`，由现有 `AdminInterceptor` 保护。
- 更新 OpenAPI 测试，确认 `/api/admin/health` 进入 `/v3/api-docs`。
- 本轮没有新增 Flyway migration。

M12 新增或变更端点：

| 方法 | 路径 | 权限 | 说明 |
| --- | --- | --- | --- |
| `GET` | `/actuator/health` | 公开 | Actuator 原生健康检查，只返回总体状态 |
| `GET` | `/actuator/info` | 公开 | Actuator info 端点 |
| `GET` | `/api/admin/health` | 管理员 | 聚合应用、MySQL、Redis、RabbitMQ 健康状态 |

验证记录：

- 聚焦测试 `mvn "-Dtest=AdminHealthServiceTest,AdminHealthControllerTest,ActuatorHealthControllerTest,OpenApiControllerTest,ArticleServiceTest" test` 通过，40 个测试全部通过。
- 完整后端测试 `mvn test` 通过，176 个测试全部通过。
- 后端编译 `mvn -DskipTests compile` 通过。
- 前端 `npm run build` 通过，Vite 构建 118 个模块。
- `git diff --check` 通过，仅有 LF/CRLF warning。

详细文档：

```text
docs/graduation/m12-actuator-health/task_plan.md
docs/graduation/m12-actuator-health/findings.md
docs/graduation/m12-actuator-health/progress.md
```

## 模块状态

以下状态按当前提交历史维护；M1-M12 均已完成并提交，不要回滚或覆盖。

| 模块 | 名称 | 状态 | 目标 |
| --- | --- | --- | --- |
| M2 | 文章草稿、审核、发布、下架状态流转 | 已完成并提交 | 让文章从直接发布升级为审核流程 |
| M3 | 收藏与基础搜索 | 已完成并提交 | 用户收藏文章，按关键词搜索已发布文章 |
| M4 | 通知中心 | 已完成并提交 | 评论、点赞、审核等事件生成站内通知 |
| M5 | 举报与内容治理 | 已完成并提交 | 用户举报文章/评论，管理员处理举报 |
| M6 | 管理员数据统计看板 | 已完成并提交 | 展示用户、文章、评论、举报、热榜等统计 |
| M7 | 用户主页与个人资料 | 已完成并提交 | 用户主页、公开文章、个人资料编辑 |
| M8 | Swagger / OpenAPI 接口文档 | 已完成并提交 | 生成接口文档，方便答辩和联调 |
| M9 | 关注与粉丝 | 已完成并提交 | 关注用户、粉丝列表、关注列表、用户主页关注统计 |
| M10 | 文件上传 | 已完成并提交 | 头像上传、文章封面上传 |
| M11 | Redis 数据定时落库 | 已完成并提交 | 定时同步浏览量、点赞数等 Redis 数据 |
| M12 | Actuator 健康检查 | 已完成并提交 | 展示应用、MySQL、Redis、RabbitMQ 健康状态 |

## 模块执行顺序

主线优先级：

```text
M1 板块分类
-> M2 文章草稿、审核、发布、下架状态流转
-> M3 收藏与基础搜索
-> M4 通知中心
-> M5 举报与内容治理
-> M6 管理员数据统计看板
-> M7 用户主页与个人资料
-> M8 Swagger / OpenAPI
```

可选增强：

```text
M9 关注与粉丝
-> M10 文件上传
-> M11 Redis 数据定时落库
-> M12 Actuator 健康检查
```

## 统一实现约束

每个模块继续使用现有项目风格：

```text
Controller
Service
Mapper
Entity
DTO
Interceptor
Config
Exception
Result<T>
```

必须保持：

- MyBatis-Plus。
- Flyway。
- Redis。
- RabbitMQ。
- JWT + Interceptor 权限方案。
- 统一 `Result<T>` 返回结构。
- Jakarta Validation。
- 简单 React / Vite 前端结构。

不要引入：

- Spring Security。
- Spring Cloud。
- Elasticsearch。
- Kafka。
- 复杂权限表。
- 复杂菜单权限。
- 分布式事务。
- Redux / Zustand。
- 复杂 UI 组件库。

## 每个模块完成标准

每完成一个模块，必须做到：

1. 新增数据库变化必须有新的 Flyway migration。
2. Entity、DTO、Mapper、Service、Controller 分层清晰。
3. 使用统一 `Result<T>`。
4. 参数使用 Jakarta Validation。
5. 权限校验正确。
6. 不返回 `password` 等敏感字段。
7. 关键异常有明确提示。
8. 补充后端测试。
9. 后端运行 `mvn test`。
10. 涉及前端时运行 `npm run build`。
11. 更新 `docs/graduation/` 下对应模块文档。
12. 输出修改文件清单、验证结果、风险和待确认事项。

## 文档目录

当前毕业设计文档：

```text
docs/graduation/
├── 毕业设计文档总览.md
├── 毕业设计进度.md
├── frontend-refactor/
├── m1-category/
├── m2-article-audit/
├── m3-favorite-search/
├── m4-notification/
├── m5-report-governance/
├── m6-dashboard/
├── m7-user-profile/
├── m8-openapi/
├── m9-follow/
├── m10-file-upload/
├── m11-redis-sync/
└── m12-actuator-health/
```

每个模块目录至少包含：

```text
task_plan.md
findings.md
progress.md
```

## 下一步

前端整体重构已按 `frontend-refactor/` 中的规划实施完成，下一步是页面人工验收、确认提交范围，再整理答辩材料。

```text
M1-M12 已完成并提交；当前工作分支已完成前端整体重构实现
```

进入提交与答辩材料阶段前需要先确认：

- 当前 `feat/frontend-refactor` 工作区是否只包含计划内改动。
- 两份未跟踪学习文档是否仍保持不动。
- 前端路由、角色识别和响应式改造是否已完成对应回归验证。
