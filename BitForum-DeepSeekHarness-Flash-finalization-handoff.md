# BitForum AI 收口任务书
## DeepSeekHarness + DeepSeek Flash 断点续作版

> 适用场景：校园网不稳定，长时间 Agent 会话可能中断；执行模型为 DeepSeekHarness 中配置的 DeepSeek Flash 类模型。
>
> 核心策略：**一次只做一个短阶段，结果立即写入仓库，完成后停止。** 新会话从磁盘读取任务书与工作日志，不依赖聊天上下文。
>
> 本轮范围只有三项：① 文档一致性审计；② AI 链路性能观察；③ 故障注入验证。

---

## 一、使用方法（项目作者先看）

### 1. 把本文件放在哪里

将本文件放到 BitForum 仓库根目录，建议保留文件名：

```text
BitForum-DeepSeekHarness-Flash-finalization-handoff.md
```

然后在仓库根目录启动 DeepSeekHarness。

### 2. 不要一次要求它做完全部任务

第一次只发送“启动提示词”，此后严格按阶段执行。每个阶段结束后，Harness 必须：

1. 更新工作日志；
2. 报告实际修改和验证结果；
3. 明确下一阶段；
4. 停止，等待你重新下令。

这样即使掉线，也不会丢失进度或让模型重复修改。

### 3. 掉线后的恢复提示词

任何时候掉线，新开会话后只发送：

```text
请在当前 BitForum 仓库中读取：
1. BitForum-DeepSeekHarness-Flash-finalization-handoff.md
2. docs/graduation/ai-agent-upgrade/closure-worklog.md

重新检查 git status 和工作日志里的证据，不要相信上一段聊天记忆。
只执行工作日志中“下一步”对应的一个阶段；完成后更新工作日志并停止。
不要 push、不要 merge、不要打 tag。
```

如果工作日志还不存在，就重新执行“阶段 0”。

---

## 二、项目背景与硬约束（Harness 必读）

BitForum 是一个 Java 17 + Spring Boot + React 的智能开发者社区毕业设计：

- M1–M12：论坛业务主体，已经完成；
- M13–M18：AI 升级，已经完成；
- 已有四类 Agent、12 个工具、RAG、内容审核、智能推荐、运营分析；
- 已有执行轨迹、用量/成本统计、统一降级和 `TokenBudgetGuard`；
- `TokenBudgetGuard` 为克制版：单用户每日 token/费用上限、超限降级、单次输入/输出保护；
- 本轮不再增加任何业务功能。

交接时曾记录以下状态，但必须从实时仓库重新核实：

- 分支：`feat/ai-agent`；
- HEAD：`514f301`；
- 相对 `main` 领先 41 个提交；
- 后端：407 项，398 通过、9 项真实 API 条件跳过；
- 前端：36 项；
- Flyway：V19；
- 工作区：干净。

已知一处明确冲突：导出上下文顶部曾是 `514f301 / 领先 41`，内嵌的 `m18-status-summary.md` 仍写 `5a385db / 领先 38`。必须定位生成或维护方式上的根因，不能只改最终导出文件。

### 绝对约束

1. 先读取仓库真实文件再判断，不凭本任务书猜代码。
2. 如有 `AGENTS.md`，必须先完整阅读并遵守。
3. 不覆盖、回退、删除项目作者已有改动。
4. 不读取、打印、记录或提交 `.env`、API Key、JWT 密钥、数据库密码。
5. 不修改历史 Flyway 迁移；本轮原则上不新增迁移。
6. 不引入 Spring Security、Spring Cloud、Elasticsearch、Kafka、Prometheus、Grafana、JMeter、K6。
7. 不改变 AI 路由、RAG、推荐排序、审核策略或论坛主业务行为。
8. 不进行全仓格式化、全仓重命名或无关重构。
9. 任何测试未实际执行时，只能写“未执行”，绝不能写“通过”。
10. 未经作者明确授权，不 commit、不 push、不 merge `main`、不打 tag。
11. 单个阶段只处理本阶段内容；发现旁支问题写入工作日志，不顺手扩项。

---

## 三、断点续作协议

### 3.1 工作日志

阶段 0 必须创建：

```text
docs/graduation/ai-agent-upgrade/closure-worklog.md
```

每完成一个小步骤就立即更新，不要等整项任务结束才写。

工作日志模板：

```markdown
# BitForum 最终收口工作日志

## 当前状态
- 当前阶段：
- 阶段状态：未开始 / 进行中 / 已完成 / 阻塞
- 最后更新时间：
- 当前分支：
- 当前 HEAD：
- 工作区摘要：
- 下一步：

## 已完成步骤
### 阶段 X
- 实际操作：
- 修改文件：
- 执行命令：
- 验证结果：
- 证据位置：
- 未解决项：

## 发现但不在本轮处理的问题
- 问题：
- 原因：
- 建议：

## 中断恢复说明
- 上一步停在哪里：
- 恢复后先检查：
- 不要重复执行：
```

### 3.2 每轮会话的固定流程

每次会话必须按以下顺序：

1. 读取本任务书、工作日志和 `AGENTS.md`；
2. 执行 `git status --short`、`git branch --show-current`、`git rev-parse --short HEAD`；
3. 对照工作日志检查文件与命令证据是否真实存在；
4. 只执行一个阶段；
5. 先写工作日志，再回复作者；
6. 回复后停止，不自动进入下一阶段。

### 3.3 中断安全

- 一个修改尽量控制在少量相关文件内。
- 长测试与真实 API 调用必须单独执行，不要和大批编辑混在同一轮。
- 修改前先列出目标文件；修改后立即运行针对性检查。
- 不依赖未保存的聊天计划；所有结论和下一步必须写进工作日志。
- 命令失败后先记录失败证据，再分析，不要无限重试。
- 同一失败最多针对明确原因修正并重试一次；仍失败则标记阻塞并停止。

---

# 四、分阶段执行提示词

以下提示词由项目作者逐段发送。**不要一次发送全部阶段。**

---

## 阶段 0：建立真实基线和工作日志

### 作者发送给 Harness

```text
执行任务书“阶段 0”。

只做只读检查和创建 closure-worklog.md，不修改业务代码与现有项目文档。
先读取 AGENTS.md（如存在）、README.md、任务书，以及 AI 升级目录中的 task_plan、progress、findings、m18-status-summary、m18-handoff。
核实分支、HEAD、工作区、相对 main 的提交数、关键文档路径和已有脚本。
把真实基线、不一致候选、阻塞条件和下一步写入 closure-worklog.md。
完成后汇报并停止，不执行阶段 1。
```

### Harness 必须执行

至少检查：

```bash
git status --short
git branch --show-current
git rev-parse --short HEAD
git log -1 --oneline
git rev-list --count main..HEAD
git remote -v
```

如仓库的默认分支或引用结构不同，应记录实际情况，不得机械套命令。未经允许不要 `git fetch`、`git pull`。

盘点以下文件是否存在，路径不同则记录真实路径：

- `README.md`
- 项目简报或上下文导出源文件
- `docs/graduation/ai-agent-upgrade/task_plan.md`
- `docs/graduation/ai-agent-upgrade/progress.md`
- `docs/graduation/ai-agent-upgrade/findings.md`
- `docs/graduation/ai-agent-upgrade/m18-status-summary.md`
- `docs/graduation/ai-agent-upgrade/m18-handoff.md`
- `docs/technical-debt.md`
- `scripts/export-ai-context.sh`

### 阶段 0 验收

- `closure-worklog.md` 已创建；
- 真实 Git 状态已记录；
- 未修改已有业务和文档；
- 下一步写为“阶段 1A：文档冲突清单”。

---

## 阶段 1A：只生成文档冲突清单

### 作者发送给 Harness

```text
执行阶段 1A：文档一致性审计。

本阶段只搜索、阅读、判断和记录冲突，不修改 README、状态文档或导出脚本。
把每个候选问题记录为：文件、位置、当前表述、证据、建议处理方式。
更新 closure-worklog.md，完成后停止，不执行修复。
```

搜索词至少包括：

```text
未开始
时间充足
TokenBudgetGuard
M16
M17
M18
5a385db
514f301
领先 38
领先 41
394
407
Redis 7
Redis Stack
V18
V19
未 push
工作区干净
```

注意：搜索结果只是候选，不允许无脑替换。历史执行记录可以保留旧状态，但必须明确它是带日期的历史记录，而不是当前状态。

### 阶段 1A 验收

- 工作日志中存在逐项冲突表；
- 区分了“真正错误”“历史记录”“易变 Git 数据”；
- 已指出上下文包混入新旧快照的可能根因，并列出证据；
- 未修改现有文档。

---

## 阶段 1B：修正文档与导出方式

### 作者发送给 Harness

```text
执行阶段 1B。

只处理 closure-worklog.md 中已经确认的文档不一致和导出脚本问题。
先列出本轮准备修改的文件，再修改。
不要修改业务代码，不要更新未经实际验证的测试数字，不要伪造 CI 状态。
完成后运行针对性检查，更新工作日志并停止。
```

修正原则：

1. M13–M18 和 `TokenBudgetGuard` 的当前状态统一为已完成。
2. `TokenBudgetGuard` 必须保留克制边界，不包装成计费平台。
3. Git HEAD、领先提交数、工作区和 push/CI 都是易变数据：
   - 优先在导出时动态生成；
   - 或写明日期和“最近一次验证快照”；
   - 避免多份长期文档重复硬编码。
4. 测试数量未重跑时写“最近一次已验证基线”，不能写成本轮新结果。
5. 修复“动态页眉是新状态、内嵌状态文档是旧快照”的根因。
6. 不破坏 Markdown 表格、代码块、链接和导出脚本的现有用途。

### 阶段 1B 验收

- 当前态文档不再出现 `TokenBudgetGuard` 未完成的表述；
- 易变状态有清晰日期或动态来源；
- 导出脚本能生成一份不自相矛盾的上下文包；
- 修改仅涉及文档/轻量脚本；
- 工作日志的下一步为“阶段 1C”。

---

## 阶段 1C：验证文档收口

### 作者发送给 Harness

```text
执行阶段 1C。

不要继续改功能。重新运行上下文导出，检查 README、status summary、handoff、progress、task plan 的当前状态是否一致。
运行 Markdown/脚本的现有检查；没有专用检查时至少检查导出命令退出码、链接路径、git diff --check 和变更范围。
把实际结果写入 closure-worklog.md，完成后停止。
```

### 阶段 1C 验收

- 新上下文包顶部与内嵌当前状态一致；
- 当前态和历史记录能区分；
- `git diff --check` 通过；
- 未验证项被明确记录；
- 下一步为“阶段 2A：性能实现调查”。

---

## 阶段 2A：性能实现调查，只出方案

### 作者发送给 Harness

```text
执行阶段 2A。

只阅读真实代码，定位 Trace、Usage、TokenBudgetGuard、RAG、Tool Calling、推荐、运营分析的真实类名、配置、接口和调用时序。
判断哪些持久化在主请求中，哪些异步；检查是否已有测试工具或配置开关。
根据代码提出最小性能观察方案，但本阶段不要写 benchmark、不要调用真实 DeepSeek、不要改生产代码。
把方案和精确文件路径写入 closure-worklog.md，完成后停止。
```

方案必须把两类数据分开：

1. **确定性本地开销**：预算查询、Trace 写入、Usage 写入；
2. **真实端到端耗时**：普通 QA、RAG、Tool Calling、推荐/运营分析。

不得用两次真实 LLM 总耗时简单相减，就声称差值是 Trace 开销。

### 阶段 2A 验收

- 方案引用了真实类名和真实调用顺序；
- 明确建议新增哪些测试/脚本，以及为什么；
- 明确是否需要生产代码改动；
- 如需要生产代码改动，必须停止并等作者确认；
- 下一步为“阶段 2B”。

---

## 阶段 2B：实现并运行确定性本地开销观察

### 作者发送给 Harness

```text
执行阶段 2B。

按工作日志中已经确认的最小方案，实现并运行“确定性本地开销”观察：预算查询、Trace 持久化、Usage 持久化。
优先使用测试代码或轻量脚本，复用现有 Service/Repository 和测试环境。
不调用真实模型，不引入性能平台，不改默认业务行为。
先预热，再保留至少 30 次原始数据，计算平均值、P50、P95、最小值、最大值。
完成后把结果和原始证据位置写入工作日志并停止。
```

要求：

- 如果写入是异步的，分别记录主线程等待时间和异步完成时间；
- 如果环境噪声导致数据明显异常，记录原因，不反复运行挑最好结果；
- 不提交临时数据库密码、Token 或用户隐私数据；
- 新增脚本必须失败时非零退出，并有使用说明。

### 阶段 2B 验收

- 至少 30 个有效样本或清楚记录为什么无法达到；
- 原始数据可追溯；
- 汇总统计可从原始数据复算；
- 没有真实 DeepSeek 调用成本；
- 下一步为“阶段 2C：真实调用确认”。

---

## 阶段 2C：真实端到端观察（执行前必须确认）

### 作者先决定

这一阶段会产生少量真实 API 调用。项目作者必须明确发送“允许执行 2C”，否则 Harness 只能记录跳过。

### 作者允许时发送

```text
允许执行阶段 2C，可以产生少量 DeepSeek API 调用费用。

按工作日志里的固定输入与真实接口执行端到端观察。每类预热 1 次，记录 5 次；不要擅自增加次数。
使用隔离测试用户和干净会话，不打印 API Key、Authorization、完整 JWT。
完成后立即写入工作日志并停止。
```

覆盖：

1. 普通 QA；
2. QA + RAG；
3. QA + Tool Calling；
4. 推荐或运营分析，按真实链路分别说明。

记录：

- 固定输入和样本量；
- 端到端耗时；
- Trace 中已有的 LLM/RAG/Tool/Persist 等步骤耗时；
- token；
- 成功、失败或降级；
- 平均值、P50、P95、最小值、最大值。

样本只有 5 次时，必须把 P95 标注为“小样本描述值”，不能宣传为稳定统计结论。

### 如果不允许费用或 API 不可用

发送：

```text
阶段 2C 不执行真实 API 调用。请在工作日志和最终报告中记录“未执行”及原因，然后进入 2D 的报告整理；不能用 mock 数据冒充真实模型性能。
```

---

## 阶段 2D：生成性能观察报告

### 作者发送给 Harness

```text
执行阶段 2D。

只基于阶段 2B、2C 的实际证据生成性能报告。不得补造数字。
建议写入 docs/graduation/ai-agent-upgrade/ai-performance-observation.md。
完成后检查所有表格、数据来源和敏感信息，更新工作日志并停止。
```

报告至少包含：

- 目的与非目标；
- 环境、版本、数据量、样本量和预热方式；
- 固定场景和执行方法；
- 原始数据位置；
- 平均值/P50/P95/最小值/最大值；
- 确定性持久化开销和真实 LLM 端到端耗时分开呈现；
- 未执行项；
- 局限与结论；
- 明确声明“本地性能观察，不代表生产 QPS、并发容量或 SLA”。

---

## 阶段 3A：故障注入实现调查，只出方案

### 作者发送给 Harness

```text
执行阶段 3A。

只阅读代码和现有测试，定位模型配置、统一降级、Trace degraded/reason、TokenBudgetGuard、Usage 统计和主业务接口。
提出两种故障注入的最小方案：A 模型运行时不可用；B 用户每日预算超限。
本阶段不要修改代码、不要改 .env、不要真正触发外部调用。
把方案、目标文件、断言和恢复方式写入工作日志，完成后停止。
```

方案 A 不能通过“把 API Key 设为空让应用启动失败”完成；目标是验证应用启动后，模型调用失败时的统一降级。

### 阶段 3A 验收

- 方案基于真实配置和类名；
- 优先使用测试 Profile、测试替身或仅当前进程的环境覆盖；
- 不需要修改/提交 `.env`；
- 明确如何验证论坛非 AI 主链路仍正常；
- 下一步为“阶段 3B”。

---

## 阶段 3B：模型运行时不可用

### 作者发送给 Harness

```text
执行阶段 3B。

按已确认方案实现并验证“应用已启动，但模型服务运行时不可用”的故障注入。
优先补自动化集成测试；需要演示脚本时保持轻量且可恢复。
不要读取或覆盖 .env，不要泄露任何凭据。
完成后更新工作日志并停止，不执行预算超限场景。
```

至少断言：

- 应用可以启动；
- 登录、文章列表或详情至少一条非 AI 主链路仍正常；
- AI 请求返回现有统一降级结构与文案；
- HTTP 状态和 `Result<T>` 符合当前约定；
- Trace 标记 degraded，并有安全、可理解的 reason；
- 前端响应不包含堆栈、密钥或底层连接详情；
- 注入配置恢复后不会继续影响正常环境。

### 阶段 3B 验收

- 自动化断言或明确可复现证据存在；
- 故障只影响 AI 能力；
- 无凭据、临时 Token 或敏感日志进入 Git；
- 下一步为“阶段 3C”。

---

## 阶段 3C：预算超限与边界

### 作者发送给 Harness

```text
执行阶段 3C。

使用测试 Profile 和隔离测试用户验证 TokenBudgetGuard：达到/超过每日阈值后，在模型调用前拦截；记录正确的降级和 Trace/Usage 口径。
同时验证等于阈值、超过阈值、未超限用户，以及单次输入/输出保护已有测试证据。
不得调用真实 DeepSeek；使用可计数的 ChatModel 测试替身证明超限请求没有进入模型。
完成后更新工作日志并停止。
```

至少断言：

- 达到/超过阈值的具体边界行为与实现一致；
- 超限时 ChatModel 调用次数不增加；
- 返回统一友好降级；
- 被拦截请求没有被记录成模型成功，也没有重复计费；
- 未超限用户仍会进入模型链路；
- 单次输入/输出保护有边界测试；
- 测试数据不会污染普通用户。

---

## 阶段 3D：生成故障注入报告

### 作者发送给 Harness

```text
执行阶段 3D。

只根据 3B、3C 的真实验证结果生成 docs/graduation/ai-agent-upgrade/ai-fault-injection-report.md。
写清注入方法、运行命令、实际结果、脱敏响应、Trace 证据、主流程证据、恢复步骤和局限。
不得补造截图、日志或测试结果。
更新工作日志并停止。
```

如新增演示脚本，必须满足：

- 不打印环境变量值；
- 不写入或覆盖 `.env`；
- 失败时非零退出；
- 不硬编码真实账号、密码、JWT 或 Key；
- 不执行 `docker compose down -v`、删库、清卷等破坏性操作；
- 明确清理测试数据和恢复方式。

---

## 阶段 4A：最终完整验证

### 作者发送给 Harness

```text
执行阶段 4A：最终验证。

先根据仓库实际脚本确认正确命令，再分别运行后端测试、前端测试、前端构建、Docker Compose 配置检查、导出脚本检查和 git diff --check。
一个命令完成后立即把结果写进 closure-worklog.md，再运行下一个，避免掉线后丢失证据。
不要 push、不要 merge、不要打 tag。完成后停止。
```

参考命令，不得盲目照抄：

```bash
mvn test

cd frontend
npm ci
npm test -- --run
npm run build

docker compose config --quiet
git diff --check
```

若测试依赖 MySQL、Redis Stack、RabbitMQ 或特定环境变量，按仓库现有说明启动和配置；不得打印敏感值。

每个验证项必须记录：

```text
命令：
退出码：
通过/失败/未执行：
关键统计：
失败原因：
证据位置：
```

---

## 阶段 4B：最终收口报告与交接

### 作者发送给 Harness

```text
执行阶段 4B。

根据 closure-worklog.md 和真实文件完成最后一次文档同步，生成或更新 final-closure-report.md。
再次检查上下文导出不存在新旧状态冲突。
最后给出改动文件、测试证据、未完成项、风险和建议提交拆分。
不要 commit、不要 push、不要 merge、不要打 tag。完成后停止。
```

建议最终报告路径：

```text
docs/graduation/ai-agent-upgrade/final-closure-report.md
```

最终报告至少包含：

- 最终核验时间、分支、HEAD、工作区状态；
- 文档不一致及处理结果；
- 性能观察报告链接和关键结论；
- 故障注入报告链接和关键结论；
- 后端、前端、构建和脚本验证结果；
- 未执行项与原因；
- “本地观察不代表生产性能”的边界；
- 是否已经具备合并 `main` 的条件。

---

## 五、Harness 每阶段统一回复格式

每次只按以下格式回复，避免长篇重复背景：

```markdown
## 阶段结果
- 阶段：
- 状态：已完成 / 部分完成 / 阻塞
- 本轮是否修改生产代码：是 / 否

## 修改文件
- `路径`：一句话说明

## 实际验证
- `命令`：通过/失败，关键数字

## 发现的问题
- 问题与证据

## 未执行
- 项目与原因

## 下一步
- 下一阶段编号与目标

已更新 `closure-worklog.md`，本轮到此停止。
```

如果阻塞，必须写清：

```text
阻塞点：
已经尝试：
证据：
需要作者决定：
安全的下一步：
```

---

## 六、最终提交前检查清单

只有全部满足，才能向作者建议合并 `main`：

- [ ] README、status summary、handoff、progress、task plan 当前状态一致；
- [ ] `TokenBudgetGuard` 已完成且边界表述一致；
- [ ] 新导出的 AI 上下文包没有新旧 Git 快照冲突；
- [ ] 性能报告只引用真实执行数据；
- [ ] 故障注入报告有可复现证据；
- [ ] 模型不可用时论坛主流程正常；
- [ ] 预算超限在模型调用前拦截；
- [ ] 后端测试结果已记录；
- [ ] 前端测试和构建结果已记录；
- [ ] Docker Compose 配置检查已记录；
- [ ] `git diff --check` 通过；
- [ ] Git 变更中没有 `.env`、密钥、JWT、密码或敏感日志；
- [ ] 所有未执行项均明确写出，未冒充通过；
- [ ] 没有擅自新增业务功能或重构。

---

## 七、明确禁止扩项

本轮不要做：

- M19 或第五个 Agent；
- Prompt CMS、管理员 AI 设置页；
- LLM-as-Judge；
- 套餐、充值、会员、余额、多租户配额平台；
- 为改善 0.0147 向量区分度而更换模型、改语料或调整推荐算法；
- 点赞反向索引；
- 洞察历史列表；
- 轨迹/用量定时归档系统；
- 微服务、Kubernetes、新监控平台；
- 全仓重命名或包名迁移；
- 与三项收口任务无关的清理、格式化和重构。

额外问题只写入工作日志的“发现但不在本轮处理的问题”，不要顺手修改。

---

## 八、第一次启动时直接复制的提示词

```text
你现在位于 BitForum 仓库根目录。

请完整读取：
1. BitForum-DeepSeekHarness-Flash-finalization-handoff.md
2. AGENTS.md（如存在）

严格使用任务书的“断点续作协议”。现在只执行阶段 0：建立真实基线和 closure-worklog.md。
不要修改现有业务代码和项目文档；不要 commit、push、merge 或打 tag。
完成后按任务书规定格式汇报并停止，不要自动进入阶段 1A。
```
