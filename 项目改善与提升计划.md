# 比特讲坛 Spring Boot 项目改善与提升计划

## 一、文档目标

这份文档不是“再学更多技术”的清单，而是帮助你把 `bit-forum-spring` 改造成一个能演示、能解释、能经得住面试追问的 Java 后端项目。

当前项目已经完成用户注册登录、文章发布、评论、Redis 浏览量与点赞、RabbitMQ 异步通知、JWT 登录认证和 Docker Compose 部署。后续改善重点放在安全性、数据一致性、可部署性、接口质量和测试覆盖上。

## 二、使用方式

- 每次只做一个小任务，不要同时改多个模块。
- 先读“涉及文件”，理解当前代码，再自己敲代码。
- 文档只给改法提示，不贴完整代码答案。
- 每完成一个任务，都运行对应验收命令，并写下“完成记录”。
- 面试准备时，重点讲清楚：我发现了什么问题、我怎么修、我怎么验证。

### 完成记录模板

```markdown
### 完成记录
- 修改日期：
- 修改文件：
- 我解决的问题：
- 我运行的验证：
- 面试时我会这样讲：
```

## 三、建议执行顺序表

| 模块 | 优先级 | 目标 | 建议完成内容 |
|---|---|---|---|
| M1 安全基础 | 第一批必须敲 | 避免安全追问露馅 | 注册不返回密码、JWT 配置外置、异常信息不泄露 |
| M2 数据一致性 | 第一批必须敲 | 保证业务数据正确 | 文章存在校验、评论校验、删除文章清理评论和 Redis |
| M3 部署可信度 | 第二批增强可信度 | 让项目更像可交付后端 | 数据库初始化、Maven Wrapper、Docker 多阶段构建 |
| M4 自动化测试 | 第二批增强可信度 | 用测试证明核心流程 | 注册登录、发布文章、点赞去重、非作者删除失败、评论不存在文章失败 |
| M5 接口规范化 | 第三批展示亮点 | 降低接口和数据库耦合 | DTO、参数校验、HTTP 状态码 |
| M6 性能与架构亮点 | 第三批展示亮点 | 准备更深入追问 | Redis ZSet 热榜、分页查询、RabbitMQ 可靠性 |

## M1 安全基础

### 1. 注册接口不返回密码哈希

**当前问题**

注册成功后，`UserController.register` 返回 `Result<User>`。`User` 实体包含 `password` 字段，响应 JSON 可能把 BCrypt 哈希返回给前端。

**为什么面试会被追问**

面试官看到“BCrypt 密码哈希”后，很容易继续问“接口会不会返回密码字段”。如果注册接口直接返回完整实体，会显得你只完成了功能，没有考虑接口安全。

**涉及文件**

- `src/main/java/com/bitforum/controller/UserController.java`
- `src/main/java/com/bitforum/entity/User.java`
- 建议新增：`src/main/java/com/bitforum/dto/UserResponse.java`

**推荐改法**

优先新增 `UserResponse` DTO，只返回必要字段，例如：

- `id`
- `username`
- `avatar`
- `createTime`

临时方案可以在 `User.password` 上加 `@JsonIgnore`，但长期更推荐 DTO，因为接口响应不应该直接暴露数据库实体。

**自己敲代码步骤**

1. 新建 `dto` 包。
2. 新建 `UserResponse`，字段只保留前端需要看的用户信息。
3. 修改 `UserController.register` 的返回类型，从 `Result<User>` 改成 `Result<UserResponse>`。
4. 注册成功后，把 `User` 转成 `UserResponse` 再返回。
5. 检查登录接口返回的 `Map` 是否仍然只包含 `token`、`userId`、`username`。

**验收标准**

- 注册响应 JSON 中不出现 `password` 字段。
- 数据库中保存的密码仍然是 BCrypt 哈希。
- 注册、登录流程仍然可用。

**面试话术**

我一开始注册接口直接返回了用户实体，后来发现这会把密码哈希也返回给前端。虽然 BCrypt 哈希不是明文密码，但它仍然属于敏感信息。所以我把接口响应改成 DTO，只返回用户 ID、用户名等必要字段，避免数据库实体直接暴露给外部。

### 2. JWT 密钥和过期时间外置

**当前问题**

`JwtUtil` 里硬编码了 JWT 密钥和过期时间。`application.yml` 中虽然有 `jwt.secret` 和 `jwt.expiration`，但当前工具类没有真正读取配置。

**为什么面试会被追问**

JWT 是登录认证核心。面试官可能会问密钥放在哪里、能不能更换、不同环境怎么配置。如果密钥硬编码在源码里，生产安全性和部署灵活性都会被质疑。

**涉及文件**

- `src/main/java/com/bitforum/util/JwtUtil.java`
- `src/main/resources/application.yml`
- 使用 JWT 的登录和拦截相关代码

**推荐改法**

- 新增 `JwtProperties`，使用 `@ConfigurationProperties(prefix = "jwt")` 读取配置。
- 将 `JwtUtil` 改成 Spring Bean，通过构造器注入 `JwtProperties`。
- `application.yml` 中使用环境变量占位，例如 `${JWT_SECRET:dev-only-secret}`。
- 确保密钥长度满足 HS256 要求。

**自己敲代码步骤**

1. 新建 `config` 或 `properties` 包。
2. 新建 `JwtProperties`，包含 `secret` 和 `expiration` 字段。
3. 在启动类或配置类上启用 `@ConfigurationPropertiesScan`。
4. 把 `JwtUtil` 的 static 密钥改成实例字段。
5. 修改调用 JWT 的地方，改为注入 `JwtUtil` Bean。
6. 用不同环境变量启动，确认 Token 仍能生成和解析。

**验收标准**

- `JwtUtil` 中没有固定密钥字符串。
- 修改 `JWT_SECRET` 后，应用使用新密钥签发 Token。
- JWT 过期时间由配置控制。

**面试话术**

JWT 的 Payload 可以被 Base64 解码看到，所以不能放敏感信息；真正防篡改依赖服务端密钥签名。我把密钥从代码里外置到配置和环境变量，避免密钥进入源码，也方便不同环境独立配置。

### 3. 异常信息不直接返回给客户端

**当前问题**

全局异常处理如果直接返回 `e.getMessage()`，可能把 SQL、表名、连接信息或内部类名暴露给前端。

**为什么面试会被追问**

岗位职责里提到“解决安全和质量问题”。异常响应是很典型的质量问题：用户需要看到稳定提示，服务端需要记录完整日志。

**涉及文件**

- `src/main/java/com/bitforum/exception/GlobalExceptionHandler.java`
- `src/main/java/com/bitforum/common/Result.java`
- 建议新增：`BusinessException`

**推荐改法**

- 新增业务异常 `BusinessException`，包含错误信息和 HTTP 状态。
- 业务异常返回明确提示，例如“文章不存在”“只能删除自己的文章”。
- 未知异常只返回“服务器内部错误”，完整异常写入服务端日志。
- 后续可逐步引入 `ResponseEntity`，让 HTTP 状态码更准确。

**自己敲代码步骤**

1. 先阅读当前 `GlobalExceptionHandler` 的返回格式。
2. 新建 `BusinessException`。
3. 在全局异常处理里分别处理业务异常、参数校验异常和未知异常。
4. 未知异常使用 `log.error` 记录堆栈。
5. 用一个故意错误的请求验证响应不泄露内部异常详情。

**验收标准**

- 未知异常响应不包含 SQL、类名、堆栈信息。
- 业务错误仍然能给用户明确提示。
- 服务端日志能看到完整异常，方便排查。

**面试话术**

我把异常分成业务异常和系统异常。业务异常可以返回明确提示，例如“文章不存在”；系统异常只给前端统一错误信息，完整堆栈留在服务端日志里，这样既方便排查，也避免内部信息泄露。

## M2 数据一致性

### 1. 浏览、点赞、取消点赞前校验文章存在

**当前问题**

`ArticleController.view`、`like`、`unlike` 直接操作 Redis。即使数据库中没有这篇文章，也能生成浏览量和点赞 Key。

**为什么面试会被追问**

这是一个很适合展示工程意识的问题：功能能跑不代表数据正确。面试官会关心 Redis 和 MySQL 之间如何保持一致。

**涉及文件**

- `src/main/java/com/bitforum/controller/ArticleController.java`
- `src/main/java/com/bitforum/service/ArticleService.java`
- `src/main/java/com/bitforum/service/RedisService.java`

**推荐改法**

在进入 Redis 操作前，先通过 `articleService.findById(articleId)` 查询文章。如果文章不存在，返回业务错误，不写 Redis。

**自己敲代码步骤**

1. 在 `view` 方法开头查询文章。
2. 文章不存在时返回失败结果，后续改造异常后可抛 `BusinessException`。
3. 在 `like` 和 `unlike` 中做同样校验。
4. 手动请求一个不存在的 `articleId`，确认 Redis 不新增对应 Key。

**验收标准**

- 不存在文章不能增加浏览量。
- 不存在文章不能点赞或取消点赞。
- 合法文章的浏览和点赞功能不受影响。

**面试话术**

我发现 Redis 是独立存储，如果不先查 MySQL，就可能给不存在的文章生成浏览量和点赞数据。所以我在操作 Redis 前先校验文章存在，避免缓存层出现脏数据。

### 2. 评论前校验文章存在

**当前问题**

`CommentService.publish` 只插入评论，没有确认 `articleId` 是否对应真实文章。

**为什么面试会被追问**

评论是文章的从属数据。如果能评论不存在的文章，说明业务约束不完整，也会影响后续查询和删除。

**涉及文件**

- `src/main/java/com/bitforum/controller/CommentController.java`
- `src/main/java/com/bitforum/service/CommentService.java`
- `src/main/java/com/bitforum/service/ArticleService.java`

**推荐改法**

推荐把文章存在校验放在 Service 层，因为评论发布是业务逻辑，不应该只依赖 Controller。

**自己敲代码步骤**

1. 在 `CommentService` 中注入 `ArticleService`。
2. `publish` 开头调用 `articleService.findById(articleId)`。
3. 文章不存在时返回业务错误或抛业务异常。
4. 后续补测试：评论不存在文章失败。

**验收标准**

- 不存在文章不能发布评论。
- 存在文章可以正常评论。
- 评论列表查询不受影响。

**面试话术**

我把评论发布的文章存在校验放在 Service 层，因为这是业务规则。这样即使以后换 Controller 或新增入口，也不会绕过校验。

### 3. 删除文章时清理评论和 Redis 数据

**当前问题**

删除文章后，相关评论、浏览量 Key、点赞 Set 可能还留着，造成 MySQL 和 Redis 数据不一致。

**为什么面试会被追问**

删除逻辑经常暴露事务意识。面试官可能会追问：删除文章时评论怎么办？Redis 数据怎么办？删除一半失败怎么办？

**涉及文件**

- `src/main/java/com/bitforum/service/ArticleService.java`
- `src/main/java/com/bitforum/service/CommentService.java`
- `src/main/java/com/bitforum/service/RedisService.java`
- `src/main/java/com/bitforum/controller/ArticleController.java`

**推荐改法**

- 删除完整流程放到 `ArticleService`。
- MySQL 中删除文章和评论使用 `@Transactional`。
- `CommentService` 增加按文章 ID 删除评论的方法。
- `RedisService` 增加 `deleteArticleData(articleId)`，删除浏览量、点赞集合和热榜数据。

**自己敲代码步骤**

1. 给 `CommentService` 增加 `deleteByArticleId`。
2. 给 `RedisService` 增加删除文章相关 Key 的方法。
3. 修改 `ArticleService.delete`，按顺序处理权限校验、删除评论、删除文章、清理 Redis。
4. 给删除流程加 `@Transactional`。
5. 手动验证删除文章后评论列表为空，Redis 相关 Key 不存在。

**验收标准**

- 作者能删除自己的文章。
- 非作者不能删除文章。
- 删除文章后，相关评论被清理。
- 删除文章后，浏览量和点赞 Redis 数据被清理。
- MySQL 删除异常时事务能够回滚。

**面试话术**

我把删除文章从简单删除一条记录，改成了一个完整业务流程：先校验作者权限，再删除评论和文章，最后清理 Redis。数据库删除部分用事务保证一致性，Redis 清理作为缓存数据处理。

## M3 部署可信度

### 1. 增加数据库初始化脚本

**当前问题**

Docker Compose 目前能创建数据库，但不能自动创建业务表。新环境启动前还需要手工建表。

**为什么面试会被追问**

如果简历写了 Docker Compose 部署，面试官可能会问“我拉下来能不能一条命令启动”。没有建表脚本会削弱可信度。

**涉及文件**

- `pom.xml`
- `src/main/resources/db/migration/`
- `docker-compose.yml`
- `README.md`

**推荐改法**

推荐引入 Flyway，添加首个迁移脚本 `V1__init_schema.sql`，创建用户、文章、评论表和必要索引。

**自己敲代码步骤**

1. 在 `pom.xml` 添加 Flyway 依赖。
2. 新建 `src/main/resources/db/migration/`。
3. 新建 `V1__init_schema.sql`。
4. 根据实体类写建表 SQL。
5. 给 `username` 添加唯一索引。
6. 删除本地测试库后启动应用，确认表自动创建。

**验收标准**

- 全新数据库启动应用后自动建表。
- 用户名唯一约束生效。
- README 不再要求手工建表。

**面试话术**

我补了数据库迁移脚本，让项目在新环境启动时自动建表。这样部署不依赖手工 SQL，表结构也能跟着代码版本管理。

### 2. 补齐 Maven Wrapper

**当前问题**

项目有 `mvnw` 和 `mvnw.cmd`，但 Maven Wrapper 文件不完整，新机器可能无法直接运行 `mvnw.cmd test`。

**为什么面试会被追问**

可交付项目应该尽量降低环境依赖。实习项目不一定要特别复杂，但构建入口要可靠。

**涉及文件**

- `.mvn/wrapper/`
- `mvnw`
- `mvnw.cmd`

**推荐改法**

使用 Maven Wrapper 标准方式补齐 `.mvn/wrapper/maven-wrapper.properties` 和必要 Wrapper 文件。

**自己敲代码步骤**

1. 查看 `.mvn/wrapper/` 当前缺少哪些文件。
2. 使用 Maven Wrapper 标准命令重新生成或补齐。
3. 在 Windows 下运行 `.\mvnw.cmd test`。
4. 在 README 中说明推荐构建命令。

**验收标准**

- 没有全局 Maven 时也能使用 Wrapper 构建。
- `.\mvnw.cmd test` 可以运行。
- `.\mvnw.cmd clean package` 可以生成 JAR。

**面试话术**

我补齐了 Maven Wrapper，这样别人拉项目后不需要先安装 Maven，也能直接运行测试和打包，降低了环境差异带来的问题。

### 3. Docker 多阶段构建

**当前问题**

当前 Dockerfile 依赖 `target/bit-forum-spring-0.0.1-SNAPSHOT.jar`，说明宿主机必须先打包，不能真正做到从源码构建镜像。

**为什么面试会被追问**

如果你说“Docker 一键启动”，面试官可能会问 Dockerfile 怎么写、是否依赖本地 target、依赖服务是否健康。

**涉及文件**

- `Dockerfile`
- `docker-compose.yml`
- `.dockerignore`
- `README.md`

**推荐改法**

使用多阶段 Dockerfile：

- 第一阶段使用 Maven 镜像构建项目。
- 第二阶段使用 JRE 镜像运行 JAR。
- Compose 中给 MySQL、Redis、RabbitMQ 添加健康检查。

**自己敲代码步骤**

1. 阅读当前 Dockerfile。
2. 改成 `builder` 阶段和 `runtime` 阶段。
3. 添加 `.dockerignore`，排除 `target`、IDE 文件和日志。
4. 在没有 `target/` 的情况下运行 `docker compose up --build`。
5. 更新 README 的启动步骤。

**验收标准**

- 删除 `target/` 后仍能通过 Docker 构建镜像。
- `docker compose up --build` 能启动后端和依赖服务。
- README 中的 Docker 说明可以实际复现。

**面试话术**

我一开始的 Dockerfile 只是复制本地已打包 JAR，后来改成多阶段构建，让镜像可以从源码直接构建。这样 Docker 部署描述更真实，也更适合新环境交付。

## M4 自动化测试

### 1. 先补 5 个核心测试

**当前问题**

当前测试基本只能证明 Spring 容器能启动，不能证明核心业务正确。

**为什么面试会被追问**

测试是证明项目质量最直接的证据。比起继续加功能，补几个能跑的核心测试更能体现工程意识。

**涉及文件**

- `src/test/java/com/bitforum/`
- `pom.xml`
- Controller、Service、Mapper 相关类

**推荐改法**

先补最关键的 5 个场景：

- 注册成功，并且响应不包含密码。
- 登录成功返回 Token。
- 发布文章成功。
- 同一用户重复点赞只计算一次。
- 非作者不能删除文章。
- 评论不存在文章失败。

**自己敲代码步骤**

1. 先写用户模块测试，再写文章模块测试。
2. Controller 测试优先用 `MockMvc`。
3. Service 单元测试可以用 Mockito。
4. 暂时不要让普通单元测试依赖本机 RabbitMQ。
5. 每补一个测试，就运行一次 `mvn test`。

**验收标准**

- `mvn test` 能通过。
- 测试失败时能定位到具体业务问题。
- 至少覆盖注册登录、文章权限、点赞去重、评论校验。

**面试话术**

我没有盲目追求测试数量，而是先覆盖最容易出问题的核心路径：认证、权限、点赞去重和评论校验。这些测试能证明项目不是只靠手工点接口跑通。

### 2. 测试隔离原则

**当前问题**

如果测试依赖本机 MySQL、Redis、RabbitMQ，就会导致别人运行 `mvn test` 很容易失败。

**为什么面试会被追问**

面试官可能会问：你的测试别人能跑吗？CI 能跑吗？中间件依赖怎么处理？

**涉及文件**

- `src/test/`
- `pom.xml`
- `application-test.yml`

**推荐改法**

- 简单单元测试用 Mockito。
- Controller 测试用 MockMvc。
- 真正需要 MySQL、Redis、RabbitMQ 的集成测试再考虑 Testcontainers。
- 为测试环境准备独立配置。

**自己敲代码步骤**

1. 新建 `application-test.yml`。
2. 给测试类加 `@ActiveProfiles("test")`。
3. 不需要中间件的测试使用 Mock。
4. 需要真实中间件的测试单独命名为集成测试。

**验收标准**

- 普通 `mvn test` 不要求手动启动 RabbitMQ。
- 测试配置和开发配置分离。

**面试话术**

我把普通单元测试和集成测试分开。普通测试不依赖外部中间件，保证任何人都能快速跑；涉及真实 Redis、MySQL、RabbitMQ 的场景再用集成测试验证。

## M5 接口规范化

### 1. 使用 DTO 区分接口模型和数据库实体

**当前问题**

Controller 中直接接收或返回 Entity，容易暴露数据库字段，也会让外部接口和数据库结构强耦合。

**为什么面试会被追问**

面试官会关注接口设计是否稳定。Entity 直接暴露出去，后面数据库字段变化可能影响前端，也可能出现越权修改字段。

**涉及文件**

- `src/main/java/com/bitforum/controller/`
- `src/main/java/com/bitforum/entity/`
- 建议新增：`src/main/java/com/bitforum/dto/`

**推荐改法**

逐步增加请求和响应 DTO，例如：

- `RegisterRequest`
- `LoginRequest`
- `PublishArticleRequest`
- `UpdateArticleRequest`
- `UserResponse`
- `ArticleResponse`

**自己敲代码步骤**

1. 先从用户注册响应 DTO 开始。
2. 再把文章发布从直接接收 `Article` 改成 `PublishArticleRequest`。
3. 更新接口参数校验。
4. 每改一个接口，就用 curl 或 MockMvc 验证一次。

**验收标准**

- 外部请求不能直接传入 `userId`、`viewCount`、`likeCount` 等服务端字段。
- 响应中不暴露不该给前端看的数据库字段。

**面试话术**

我把 Entity 和 DTO 分开，是为了让数据库结构和接口模型解耦。这样既能避免敏感字段泄露，也能防止用户通过请求修改服务端字段。

### 2. 补充参数校验

**当前问题**

部分接口缺少输入长度、格式和正数校验，错误输入可能进入 Service 或数据库。

**为什么面试会被追问**

参数校验是后端接口质量的基础。岗位要求里提到联调和质量问题，校验能减少前后端联调时的异常情况。

**涉及文件**

- DTO 类
- Controller 类
- `GlobalExceptionHandler`

**推荐改法**

使用 Bean Validation：

- 用户名长度和格式。
- 密码最小长度。
- 标题和内容不能为空。
- 评论不能为空并限制最大长度。
- ID 必须为正数。

**自己敲代码步骤**

1. 在请求 DTO 字段上加校验注解。
2. Controller 参数加 `@Valid`。
3. 全局异常处理里处理参数校验异常。
4. 用非法参数请求接口，确认返回统一错误格式。

**验收标准**

- 无效输入在进入 Service 前被拒绝。
- 参数错误响应格式统一。
- 错误信息对前端可理解。

**面试话术**

我把参数校验前置到 Controller 层，非法输入不会进入业务逻辑。这样可以减少脏数据，也让前端联调时更容易定位问题。

### 3. 使用更准确的 HTTP 状态码

**当前问题**

当前接口多使用统一 `Result` 表达成功失败，但 HTTP 状态可能不够准确。

**为什么面试会被追问**

REST API 不只看响应体，也看 HTTP 状态。状态码准确能让前端、测试和网关更容易处理错误。

**涉及文件**

- Controller 类
- `Result`
- `GlobalExceptionHandler`

**推荐改法**

逐步引入：

- 参数错误：400
- 未登录：401
- 无权限：403
- 资源不存在：404
- 未知异常：500

**自己敲代码步骤**

1. 先从全局异常处理改起。
2. 业务异常中携带 HTTP 状态。
3. Controller 中少写重复错误判断，交给异常处理统一返回。
4. 用 Postman 或 curl 查看 HTTP 状态码。

**验收标准**

- 资源不存在返回 404。
- 非作者删除文章返回 403。
- 未登录访问受保护接口返回 401。

**面试话术**

我在统一响应结构之外，也补了 HTTP 状态码。这样前端不需要只解析响应体，也能根据状态码做统一处理。

## M6 性能与架构亮点

### 1. 热榜改为 Redis ZSet

**当前问题**

当前热榜接口会读取全部文章，再逐个查询浏览量和点赞数，最后在应用内排序。数据多了以后性能会变差。

**为什么面试会被追问**

Redis 热榜是项目亮点。面试官很可能问为什么用 Redis、为什么用 Set、热门排行怎么做。

**涉及文件**

- `src/main/java/com/bitforum/controller/ArticleController.java`
- `src/main/java/com/bitforum/service/RedisService.java`

**推荐改法**

- 用 Redis Set 保持点赞去重。
- 用 Redis String 保存浏览量。
- 用 Redis ZSet 保存热度分数。
- 浏览和点赞时更新 ZSet 分数。
- 热榜接口只取 Top N 文章 ID，再查文章详情。

**自己敲代码步骤**

1. 确认 `RedisService` 中已有 ZSet 方法。
2. 在浏览和点赞成功后更新热度。
3. 修改热榜接口，优先从 ZSet 取 Top N。
4. 查不到文章时跳过脏 ID。
5. 保留一个简单权重规则，例如 `浏览 + 点赞 * 3`。

**验收标准**

- 热榜接口支持 Top N。
- 不再每次遍历所有文章。
- 浏览和点赞会影响热榜排序。

**面试话术**

点赞去重用 Redis Set，因为同一个用户只能出现一次；热榜用 ZSet，因为它天然支持分数排序和 Top N 查询。这样比每次查全量文章再排序更适合数据量增长。

### 2. 分页查询

**当前问题**

文章和评论接口一次返回全部数据，数据量增加后会影响接口性能。

**为什么面试会被追问**

列表接口几乎都需要分页。没有分页会被认为只适合 Demo，不适合真实使用。

**涉及文件**

- 文章列表接口
- 评论列表接口
- MyBatis-Plus 分页配置

**推荐改法**

使用 MyBatis-Plus 分页插件，文章和评论列表都支持 `page`、`size`。

**自己敲代码步骤**

1. 添加 MyBatis-Plus 分页配置。
2. 修改文章列表接口，支持页码和每页数量。
3. 修改评论列表接口，支持分页。
4. 限制最大 `size`，避免一次查太多。

**验收标准**

- 列表接口返回总数、当前页、每页数量和数据列表。
- 超大 `size` 会被限制。

**面试话术**

我给文章和评论列表加了分页，避免一次性返回全部数据。分页结果包含总数和当前页信息，前端可以直接做分页组件。

### 3. RabbitMQ 可靠性增强

**当前问题**

项目最初只是在文章发布后发送简单字符串消息，例如“文章ID：xx”。这种做法能完成异步通知，但消息格式不稳定，也缺少发送确认、重复消费处理、失败消息兜底等可靠性设计。

**为什么面试会被追问**

只要简历写 RabbitMQ，面试官通常会问为什么不用线程池、如何防止消息丢失、如何处理重复消费。

**涉及文件**

- `src/main/java/com/bitforum/config/RabbitMQConfig.java`
- `src/main/java/com/bitforum/service/ArticleService.java`
- `src/main/java/com/bitforum/service/NotificationListener.java`
- `src/main/java/com/bitforum/message/ArticlePublishMessage.java`
- `src/main/java/com/bitforum/service/RedisService.java`
- `src/main/resources/application.yml`
- `src/test/java/com/bitforum/service/ArticleServiceTest.java`
- `src/test/java/com/bitforum/service/NotificationListenerTest.java`
- `src/test/java/com/bitforum/service/RedisServiceTest.java`

**已完成改造**

- 将文章发布消息从字符串改为 `ArticlePublishMessage` 结构化对象。
- 消息中包含 `messageId`、`articleId`、`userId`、`title`、`publishTime`。
- 使用 `DirectExchange + RoutingKey + Binding` 显式声明消息路由关系。
- 开启生产者 Confirm 和 ReturnsCallback，记录消息是否到达 Broker、是否成功路由到队列。
- 使用 `Jackson2JsonMessageConverter`，让 Java 消息对象和 JSON 自动转换。
- 消费者改为手动 ACK：成功 `basicAck`，失败 `basicNack`。
- 使用 Redis Set 记录已处理 `messageId`，避免重复消费。
- 配置 DLX / DLQ，让失败消息进入死信队列。
- 增加 DLQ 监听器，记录死信消息，便于后续排查、告警或人工补偿。

**关键链路**

正常消息链路：

```text
ArticleService
-> article.exchange
-> routingKey: article.publish
-> article.publish.queue
-> NotificationListener
-> basicAck
```

重复消息链路：

```text
NotificationListener
-> Redis Set 判断 messageId 已处理
-> 跳过业务
-> basicAck
```

失败消息链路：

```text
NotificationListener 处理失败
-> basicNack(requeue=false)
-> article.dlx.exchange
-> routingKey: article.publish.dlq
-> article.publish.dlq
-> handlePublishDeadLetter 记录日志
```

**验收标准**

- 文章发布后能发送结构化 `ArticlePublishMessage`。
- 生产者发送到 Broker 成功或失败有日志记录。
- 路由失败会触发 ReturnsCallback 日志。
- 重复 `messageId` 不会重复执行业务。
- 消费成功后手动 `basicAck`。
- 消费失败后 `basicNack(requeue=false)`，失败消息进入 DLQ。
- `mvn test` 能通过，当前已覆盖 MQ 发送消息对象、Redis 幂等和重复消息 ACK 场景。

**排查记录**

本地 RabbitMQ 已存在旧 `article.publish.queue` 时，新增 DLX 参数会报：

```text
PRECONDITION_FAILED - inequivalent arg 'x-dead-letter-exchange'
```

原因是 RabbitMQ 不允许同名队列原地修改参数。处理方式是删除旧队列，让 Spring Boot 按新配置重新声明队列。

**面试话术**

我使用 RabbitMQ 是为了把文章发布和通知逻辑解耦。生产者保存文章后发送 `ArticlePublishMessage`，消息里包含 `messageId`、`articleId`、`userId`、`title` 和 `publishTime`。消息先发送到 `article.exchange`，通过 `article.publish` 路由到 `article.publish.queue`。生产者侧开启 Confirm 和 ReturnsCallback，用来观察消息是否到达 Broker、是否成功路由。消费者侧使用手动 ACK，处理成功后 `basicAck`，失败时 `basicNack(requeue=false)`，失败消息会进入 DLQ。为了避免重复消费，我用 Redis Set 记录已处理过的 `messageId`，重复消息直接 ACK 跳过。

## 七、最终验收清单

完成全部改善后，项目应达到以下状态：

- 注册、登录、文章、评论、点赞核心流程能跑通。
- 注册和其他用户响应不暴露 `password`。
- JWT 密钥和过期时间不硬编码在工具类中。
- 不存在文章不能浏览、点赞或评论。
- 删除文章后，评论和 Redis 数据有明确清理策略。
- 全新数据库可以自动初始化表结构。
- 删除 `target/` 后也能通过 Docker 构建后端镜像。
- `mvn test` 覆盖核心业务路径。
- README 中的启动步骤能现场验证。
- 每个关键改动都有自己的面试说法。

## 八、项目总话术

面试介绍项目时，可以围绕这句话展开：

我做了一个 Spring Boot 论坛项目，从 JDBC 版升级到 Spring Boot 版，实现了用户登录、文章发布、评论、Redis 点赞和浏览量、RabbitMQ 异步通知以及 Docker Compose 部署。后续我重点补强了安全、数据一致性、部署和测试，比如注册接口不返回密码哈希、浏览点赞前校验文章存在、JWT 密钥外置、补数据库初始化和核心测试。这个项目让我完整经历了接口开发、前后端联调、缓存设计、异步解耦和部署排查。

不要把项目讲成“我用了很多技术”。更好的讲法是：

- 我为什么用这个技术。
- 我遇到了什么问题。
- 我怎么修。
- 我怎么验证。
- 这个改动让项目质量提升在哪里。
