# 比特讲坛 — 简历项目描述

## 项目概述

基于 Spring Boot 3 开发的轻量级论坛系统，支持用户注册登录、文章 CRUD、评论互动、点赞排行、异步通知、JWT 认证，全部服务通过 Docker 容器化部署。

- **项目周期：** 12 周（独立开发）
- **代码规模：** 25 个 Java 文件，13 个 RESTful API
- **技术栈：** Java 17 + Spring Boot 3.4.5 + MyBatis-Plus 3.5.9 + MySQL 8.0 + Redis 7 + RabbitMQ 3 + Docker

---

## 简历亮点（可选 3-5 条使用）

1. 基于 Spring Boot 3 + MyBatis-Plus 开发论坛 RESTful API，完成用户注册登录（BCrypt 加密）、文章 CRUD、评论等核心模块；统一 Result 返回格式 + @RestControllerAdvice 全局异常处理 + @Valid 参数校验，保证接口健壮性

2. 使用 Redis 的 String 与 Set 数据类型实现文章浏览量实时计数、用户点赞去重、热门文章加权排行（浏览量 + 点赞数 × 3），高频读写操作下沉到缓存层，降低 MySQL 压力

3. 引入 RabbitMQ 消息队列替代线程池处理文章发布后的异步通知，实现生产者与消费者解耦、消息持久化，提升系统可靠性

4. 基于 JWT + 自定义 HandlerInterceptor 实现无状态用户认证，配合 @RequestAttribute 从 Token 自动解析用户身份，替代手动传递 userId 的不安全方式，配合 BCrypt 密码加密，保证存储和传输双重安全

5. 使用 Docker + Docker Compose 将 Spring Boot 应用与 MySQL、Redis、RabbitMQ 四个服务容器化，通过环境变量实现跨容器网络通信，一条命令完成全栈部署

---

## 面试可展开的方向

- **为什么点赞用 Set 而不是 String？** Set 天然去重，SISMEMBER 判断是否已赞，SREM 精确取消，比 String incr/decr 更安全
- **JWT 的签名是加密吗？** 不是，签名只是防篡改。Payload 是 Base64 编码，任何人可解码查看，所以不能存敏感信息
- **拦截器和过滤器的区别？** 拦截器属于 Spring MVC，可以拿到 Controller 方法信息；过滤器属于 Servlet 容器，更底层，Spring Security 用的就是 Filter
- **线程池换成 RabbitMQ 的好处？** 线程池是内存级的，服务重启任务丢失；MQ 持久化到磁盘，支持消息重试，天然跨服务
- **Docker Compose 里容器怎么互相发现？** 同一 docker-compose 内服务名即域名，Docker 内置 DNS 自动解析，不需要配 IP
- **BCrypt 每次加密结果为什么不同？** 自动掺入随机盐（Salt），盐值嵌在密文中，验证时自动提取，不需要单独存盐值字段
